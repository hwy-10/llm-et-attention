# 프로젝트 가이드북

**MSB-우선 비트평면 어텐션 스코어 유닛 + 조기 종단 제어기**
종합설계 · 2026-08-30

```
   테스트 424 통과 / 0 실패     crosscheck 12/12     실험 exp1~exp9 정상
   상한식 검사 16,785,408건 위반 0
```

> **이 문서 하나만 읽으면 프로젝트 전체가 파악됩니다.**
> 배경지식 → 원리 → 아키텍처 → 코드 → 분업 → 결과 순으로, **개발 흐름대로** 정리했습니다.
> 더 깊이 들어갈 때 볼 문서는 각 절 끝에 적어 두었습니다.

---

## 목차

| # | 절 | 누가 읽나 |
|---|---|---|
| [0](#0-5분-요약) | 5분 요약 | 전원 |
| [1](#1-배경지식--왜-이걸-만드나) | 배경지식 — 왜 이걸 만드나 | 전원 |
| [2](#2-원리--어떻게-동작하나) | 원리 — 어떻게 동작하나 | 전원 |
| [3](#3-아키텍처--하드웨어-구조) | 아키텍처 — 하드웨어 구조 | RTL 담당 |
| [4](#4-코드-지도--파일별-역할) | 코드 지도 — 파일별 역할 | 전원 |
| [5](#5-분업과-인계--두-팀의-경계) | 분업과 인계 — 두 팀의 경계 | 전원 |
| [6](#6-실전--3가지-함정) | 실전 — 3가지 함정 | 전원 |
| [7](#7-최종-결과물) | 최종 결과물 | 전원 |
| [8](#8-직접-돌려보기) | 직접 돌려보기 | 실험 담당 |
| [9](#9-남은-일과-정직하게-짚을-것) | 남은 일과 정직하게 짚을 것 | 전원 |
| [부록](#부록-a-문서-지도) | 문서 지도 · 용어집 | 전원 |

---

## 0. 5분 요약

### 뭘 만드나

LLM이 글자를 하나 만들 때마다, **지금까지 나온 모든 토큰**과 내적을 합니다. 문맥이 2,048이면 2,048번. 이게 매 글자마다 반복됩니다.

그런데 실제로 쓰이는 건 **상위 몇 개**뿐입니다. softmax를 거치면 나머지는 0으로 눌립니다.

> **그럼 왜 전부 계산하나?**

**답을 잃지 않으면서 도중에 그만두는 하드웨어**를 만듭니다.

### 어떻게

```
   K 를 비트 단위로 잘라 8장의 "평면" 으로 저장한다
   상위 비트(MSB)부터 한 장씩 처리한다
   몇 장만 봐도 최종 점수의 범위 [L, U] 를 알 수 있다
   범위 전체가 기준선 아래로 내려간 토큰은  ->  버린다
```

**곱셈기를 쓰지 않고, 상위 k개를 하나도 잃지 않습니다.**

### 결과

| 지표 | 값 |
|---|---|
| **실효 speedup** | **1.111배** (근사 모드 1.290배) — Fmax 저하 반영 |
| **손익분기** | **T ≈ 1,280** |
| 읽기 절감 (T=2048) | 33.3% (margin 0.70이면 45.5%) |
| **perplexity 열화** | **+2.78%** — 하드 top-k(+3.63%)보다 좋다 |
| 무손실 margin | 0.40 (384조합 수렴) |
| DSP 슬라이스 | **0개** |

---

## 1. 배경지식 — 왜 이걸 만드나

### 1-1. 연산보다 데이터 이동이 비싸다

```
   32비트 덧셈           0.9 pJ
   DRAM 에서 32비트 읽기  640 pJ      <- 700배
```

칩 안에서 곱하는 것보다 **메모리에서 값을 꺼내오는 게 훨씬 비쌉니다.** 이걸 "메모리 월"이라 부릅니다.

### 1-2. LLM 추론은 두 단계다

| 단계 | 하는 일 | 성격 |
|---|---|---|
| **Prefill** | 입력 문장 전체를 한 번에 처리 | 연산 집약 |
| **Decode** | 글자를 하나씩 생성 | **메모리 집약** ← 우리 목표 |

Decode에서는 새 질의 벡터 하나(`q`)로 **KV 캐시 전체**를 훑습니다. 재사용이 없어서 **읽어온 데이터를 한 번 쓰고 버립니다.**

```
   연산 강도 = 연산 횟수 / 메모리 바이트

   Prefill    높다   ->  연산기가 바쁘다
   Decode     ~1     ->  메모리가 놀지 않고 연산기가 논다
```

### 1-3. KV 캐시가 얼마나 큰가

Llama 3.2 1B, FP16, 문맥 2,048 기준:

```
   2 (K,V) × 16층 × 8 KV헤드 × 64차원 × 2048토큰 × 2바이트 ≈ 134 MB
```

**온칩 SRAM에 안 들어갑니다.** 매 글자마다 이걸 읽어야 합니다.

### 1-4. 어텐션의 두 가지 성질

**① 점수 분포가 매우 치우쳐 있다**

softmax 후 상위 몇 개가 확률의 대부분을 가져갑니다. 실측에서 **T=2048일 때 상위 16개면 충분**했습니다(뒤 §7).

**② 비트마다 중요도가 다르다**

INT8에서 MSB 하나가 LSB 128개만큼의 값어치를 합니다.

```
   0b1 0 0 0 0 0 0 0  =  128
   0b0 1 1 1 1 1 1 1  =  127      <- 7비트를 다 합쳐도 MSB 하나보다 작다
```

### ★ 두 성질을 합치면

> **상위 비트만 봐도 "이 토큰은 상위 k에 못 든다"를 판정할 수 있다.**

이게 이 프로젝트의 출발점입니다.

> 더 깊이: `배경지식_가이드.pdf` (팀원 배포용, 개념 설명 중심)

---

## 2. 원리 — 어떻게 동작하나

### 2-1. 비트평면 분해 — 곱셈기가 사라진다

`K`를 INT8로 양자화하고 **비트 자리별로 잘라** 재배치합니다.

```
   K[j][i] = 0b 1 0 1 1 0 0 1 0
                │ │ │ │
                │ │ │ └── 평면 3 (자리값 2^4)
                │ │ └──── 평면 2 (자리값 2^5)
                │ └────── 평면 1 (자리값 2^6)
                └──────── 평면 0 (자리값 2^7)   ← MSB, 가장 무겁다
```

메모리에는 **평면 우선(plane-major)** 으로 저장합니다. 평면 0에는 **모든 토큰의 최상위 비트만** 모여 있습니다.

그러면 내적이 자리값별 합으로 쪼개집니다.

```
   s_j = Σ_b  2^b · P_b(j)

   P_b(j) = Σ  q_i        (K[j][i] 의 b번째 비트가 1인 i 에 대해서만)
```

**`P_b`는 "비트가 1인 자리의 q를 더한 값"입니다.** 비트는 0 아니면 1이니 **곱하기가 아니라 고르기**입니다.

```
   하드웨어  =  AND 게이트 한 줄  +  가산 트리
```

자리값 곱하기도 시프트입니다.

```
   S_m ← (S_m << 1) + P_b        MSB부터 넣으면 자리가 저절로 맞는다
```

**DSP 슬라이스 0개.** 대신 8번 나눠 하니 **연산 사이클은 8배**입니다. 이 대가를 뭘로 갚는지는 §6-3.

> **정직하게** — "곱셈 없음"은 **토큰마다 반복되는 구간**에 대한 주장입니다. `q`에 스케일을 접는 부분과 zero-point 보정에는 곱셈이 남지만, **스텝당 한 번**이고 토큰 수에 비례하지 않습니다.

### 2-2. 구간이 좁아진다

MSB부터 처리하니, 몇 장만 봐도 **최종 점수가 어디쯤일지** 압니다.

```
   남은 비트가 최선이면   양수 q 만 다 더해진다     Q+ = Σ(q > 0)
   남은 비트가 최악이면   음수 q 만 더해진다        Q− = Σ(q < 0)
```

```
   W_m = 2^(8−m) − 1                  남은 비트의 최대 가중치

   L_m = S_m + W_m·Q−   ≤   s_j   ≤   S_m + W_m·Q+ = U_m
```

`W_m`이 평면마다 반씩 줍니다.

```
   평면 m    0     1    2    3    4   5   6   7   8
   W_m     255   127   63   31   15   7   3   1   0
```

**평면 하나 볼 때마다 불확실성이 절반.** 마지막엔 `W_8 = 0`이라 **구간이 점으로 수렴**합니다 — 그게 정확한 답입니다.

`W_m = 2^n − 1` 꼴이라 곱셈이 아니라 **시프트 후 뺄셈**입니다.

```
   W_m · X  =  (X << (8−m)) − X
```

**`Q+`와 `Q−`는 `q`에만 의존**하므로 스텝 시작에 한 번만 계산합니다. 하드웨어에선 레지스터 두 개입니다.

### 2-3. θ — 설계에서 가장 중요한 정의

기준선을 이렇게 잡습니다.

```
   θ = 살아있는 토큰들의 하한(L_m) 중  k번째로 큰 값
```

판정은 한 줄입니다.

```
   U_m(j) < θ   →   토큰 j 를 버린다
```

#### 왜 안전한가 — 두 줄 증명

1. `θ`가 **k번째로 큰 하한**이므로, **하한이 θ 이상인 토큰이 최소 k개** 있습니다.
   그 토큰들의 진짜 점수는 하한 이상이니 **전부 θ 이상**입니다.
2. 어떤 토큰의 **상한이 θ보다 작으면**, 진짜 점수도 θ보다 작습니다.
   위의 k개를 **절대 못 이깁니다.**

**따라서 버려도 상위 k를 안 잃습니다. 근사가 아니라 정확합니다.**

#### ★ 여기가 선행연구와 갈리는 지점

```
   PADE (HPCA 2026)   θ = max(하한) − α·radius
   BitStopper         η = max(A^r) − α·radius
   BETA               MTS (max-based threshold selection)
   ────────────────────────────────────────────────────
   우리               θ = 활성 하한 중 k번째로 큰 값
```

**계보 전체가 `max(하한) − α·radius`를 씁니다.** 1등 하한을 기준 삼고 휴리스틱하게 푸는 거라 **top-k 보장이 안 나옵니다.** 그래서 PADE 자신도 무손실이라 하지 않고 *"standard(0% loss) / aggressive(1% loss)"* 두 설정으로 제시합니다.

우리는 k번째를 써서 **증명이 됩니다.**

#### 왜 "확정된 점수"로 하면 안 되나

| 후보 | 정의 | 결과 |
|---|---|---|
| A | k번째로 **확정된** 점수 | **절대 안 걸린다** |
| B | 활성 토큰 **하한** 중 k번째 | **채택** |

**평면 우선**으로 처리하기 때문입니다 — 모든 토큰의 평면 0을 하고, 그다음 평면 1을 합니다. 마지막 평면 전까지 **어떤 토큰도 점수가 확정되지 않습니다.** A는 영원히 안 걸립니다.

### 2-4. 안전장치 두 개

**keep-top-k 가드** — 생존자가 `k`개 미만이 되려 하면 상한이 큰 순으로 되살립니다.

> **전부-아니면-전무로 건너뛰면 안 됩니다.** 큰 margin에서 가지치기가 통째로 무효화되어 근사 모드가 동작하지 않습니다.

**margin** — 조건을 `U_m < θ + margin`으로 세게 잡으면 더 버립니다. 무손실은 아니지만 절감이 큽니다.

### 2-5. 세 파라미터의 뜻

| 파라미터 | 무엇을 정하나 | 채택값 |
|---|---|---|
| `K_TOP` | **몇 개를 살릴 건가** | **16** |
| `MARGIN` | **얼마나 세게 버릴 건가** | **0.40** (운영 0.70) |
| `margin_mode` | 그 "얼마나"의 **단위** | **`relative_width`** |
| `M0` | **θ를 언제 정할 건가** | **4** |

**`margin_mode`가 왜 필요한가** — `margin = 0.5`라고만 하면 "0.5가 뭐냐"가 문제입니다. 점수 5,000점짜리와 500점짜리에서 뜻이 다르니까요.

```
   relative_width   불확실성 폭 U−L 에 비례      ★ 채택
   relative_gap     1등 하한 − θ 에 비례        margin=1.0 에서 구조적 포화
   relative_qpos    255·Q+ 에 비례              평면 무관 고정
   absolute         점수 그대로
```

`relative_width`는 구간 폭이 평면마다 절반씩 주니 **초반엔 세게, 후반엔 약하게** 자동 조절됩니다.

**`M0 = 4`인 이유** — 실측에서 **평면 3부터 종단이 시작**합니다. 그 전에 θ를 정해 봐야 소용없습니다.

```
   평면    종단 비율
    0~2      0.0%      구간이 너무 넓어 아무도 안 죽는다
     3      11.3%      여기서 시작
     4      57.1%      최빈
     5      22.6%
```

> 더 깊이: `ARCHITECTURE.md` §2 (알고리즘 정의), §4 (θ 정의)

---

## 3. 아키텍처 — 하드웨어 구조

### 3-1. 블록 다이어그램

`[1]` = 팀 1 소유, `[2]` = 팀 2 소유

```
[1] kv_write_transpose   새 토큰 K(64×8b) 를 8개 평면에 1비트씩 흩어 기록
                         전치 버퍼로 64×8 -> 8×64 재배열.  스텝당 1회
         │
         ▼
[2] q_prep               q 를 싣고 Q+ / Q− 를 한 번 계산해 레지스터에
                         이 두 값이 이후 8평면 내내 상·하한을 만든다
         │
         ▼
 ┌── 평면 m = 0(MSB) … 7  반복 ────────────────────────────────────┐
 │                                                                │
 │ [1] kv_plane_mem    평면 m 의 비트를 읽는다                       │
 │                     active 마스크로 읽기 요청 자체를 막는다        │
 │                     ← ★ 절감이 실제로 발생하는 지점               │
 │        │                                                       │
 │        ▼                                                       │
 │ [1] masked_sum      비트가 1인 자리의 q 를 더해 P_b               │
 │                     AND + 64입력 가산 트리.  LANES=32개 인스턴스  │
 │        │                                                       │
 │        ▼                                                       │
 │ [1] shift_acc       S_m ← (S_m<<1) + P_b                       │
 │                     22b 누산기 × N_MAX.  ★ 출력 단계에서 재사용    │
 │        │                                                       │
 │        ▼                                                       │
 │ [2] bound_calc      시프트-뺄셈으로 L_m, U_m.  곱셈기 없음        │
 │        │                                                       │
 │        ▼                                                       │
 │ [2] theta_engine    활성 하한 중 k번째로 큰 값.  부분 정렬 회로    │
 │                     theta_valid 를 함께 낸다                     │
 │        │                                                       │
 │        ▼                                                       │
 │ [2] term_ctrl       U_m < θ 인 토큰의 active 비트를 내린다        │
 │                     keep-top-k 가드 포함.  ★★ 핵심 블록          │
 │        │                                                       │
 │        └──── 되먹임: 다음 평면 읽기를 막는다 ────────────────────┤
 └────────────────────────────────────────────────────────────────┘
         │
         ▼
[2] score_out            active 를 훑으며 S_8 을 읽어 스트림 출력
                         zero-point 보정을 여기서 한 번만.  새 저장소 0
```

**점선 안이 되먹임 루프입니다.** `term_ctrl`의 판정이 `kv_plane_mem`의 읽기를 막고, 그 안 읽은 만큼이 이득입니다.

### 3-2. 상태 기계

```
IDLE → LOAD_Q → WRITE_K → INIT → SCAN_PLANE ⇄ BOUND_CHECK → OUTPUT → IDLE
                                      ↑______m < 7______│
```

**(질의, 헤드) 하나를 한 번에 처리합니다.** 헤드는 순차입니다 — 이게 §9의 "헤드별 MARGIN 테이블이 싸다"는 근거입니다.

### 3-3. 비트폭

`q`가 INT8, `D = 64` 기준입니다. 골든모델 실측으로 전부 여유가 확인됐습니다.

| 신호 | 범위 | 폭 | 유도 |
|---|---|---:|---|
| `q_i` | −128 … 127 | 8b | 입력 |
| `P_b` | −8,192 … 8,128 | **14b** | 64 × q 범위 |
| `S_m` | −2,088,960 … 2,072,640 | **22b** | 255 × P_b |
| `Q+` / `Q−` | 0…8,128 / −8,192…0 | 13b / 14b | 64 × q |
| `U_m`, `L_m`, `θ` | −4,177,920 … 4,145,280 | **24b** | S_m + W_m·Q |

### 3-4. θ의 "미확정" 상태

활성 토큰이 `k`개 미만이면 θ가 정의되지 않습니다. 골든모델은 `−inf`를 쓰지만 24b 정수엔 `−inf`가 없습니다.

```
   ✗ A안   24b 최솟값을 −inf 로 약속        "θ가 낮다" 와 "θ가 없다" 가 구분 안 됨
   ✓ B안   theta_valid 비트를 따로 둔다      채택
```

```verilog
   if (theta_valid && active[j] && U_m(j) < theta_eff)  active[j] <= 0;
```

### 3-5. 제어/상태 레지스터

| 주소 | 이름 | 접근 | 설명 |
|---|---|---|---|
| 0x00 | `CTRL` | R/W | bit0 = start |
| 0x04 | `MODE` | R/W | bit0 = 종단 사용, bit1 = θ 자동/고정 |
| 0x08 | `THETA_FIX` | R/W | Phase 1 고정 θ |
| **0x0C** | **`MARGIN`** | R/W | **기본 0.40**(무손실) / **운영 0.70**(99.5%) |
| 0x10 | `M0` | R/W | θ 확정 시점 |
| 0x14 | `K_TOP` | R/W | 상위 k 크기 |
| 0x20 | `CNT_CYCLE` | R | 스텝 소요 사이클 |
| **0x24** | **`CNT_READ`** | R | **평면 읽기 횟수 (핵심 측정값)** |
| 0x28 | `CNT_ALIVE` | R | 평면별 생존 토큰 수 누적 |
| 0x2C | `CNT_TRUNC` | R | 출력 잘린 스텝 수. **0이 아니면 논문에 보고** |

> `MARGIN`이 **R/W 레지스터**인 게 중요합니다. 합성 상수가 아니라 **런타임에 바꿉니다.** 그래서 "무손실 0.40"과 "공격적 0.70" 중 하나를 고를 필요가 없습니다.

### 3-6. 확정 파라미터

| 기호 | 값 | 근거 |
|---|---|---|
| `D` | 64 | Llama 3.2 1B head_dim |
| `B` | 8 | INT8 = 평면 8장 |
| `N_MAX` | **2048** | 512는 지는 구간. BRAM 추가 0 |
| `K_TOP` | **16** | 실측 perplexity (§7) |
| `LANES` | 32 | 토큰 병렬도 |
| `M0` | 4 | 생존 곡선상 평면 4부터 |
| `WORD_TOKENS` / `N_PORTS` | **1 / 32** | `곱 ≥ LANES` 여야 한다 |
| `MARGIN` | **0.40** (운영 0.70) | 384조합 수렴 |
| `IDX_BITS` | **11** | `log2(N_MAX)` |
| `LATENCY_MODE` | **auto** | 지연은 `T`의 함수 |

> **함께 움직여야 하는 3쌍**
> ```
> seq_len(N_MAX)  ↔  out_buf, idx_bits      어긋나면 예외
> word_tokens     ↔  n_bram_ports           곱이 lanes 이상
> K_TOP           ↔  out_buf                out_buf ≥ K_TOP
> ```
> 테스트가 검사합니다. 하나만 고치면 `tests/run_tests.py`가 잡습니다.

> 더 깊이: `ARCHITECTURE.md` (비트폭 유도, 메모리 맵, 상태기계, 레지스터 전체)

---

## 4. 코드 지도 — 파일별 역할

골든모델은 **Python/NumPy 3,351줄**입니다. RTL을 쓰기 전에 **알고리즘이 맞는지, 절감이 실제로 나오는지** 확인하는 도구입니다.

### 4-0. 전체 흐름

```
   [2] dataset / model_hooks      q, K 준비
   [1] quantize                   K → 비트평면            ★ 인계 1
   [1] accumulator                scale 접기
   [1] masked_sum                 P_b 계산                ★ 인계 2
   [2] bounds                     Q+ / Q−
        ┌─ 평면 반복 ─────────────────────────────┐
        │ [1] accumulator   S 누산                 │
        │ [2] bounds        L, U                   │
        │ [2] threshold     θ                      │
        │ [2] terminator    판정 → read_live       │
        │      └── 되먹임: 다음 평면 읽기 차단 ─────┘
        └─────────────────────────────────────────┘
   [2] output_stage               생존 집합 출력
   [1] memory / schedule          회계                    ★ 인계 3
   [2] designs / decode_loop      4설계 비교
```

### 4-1. 준비 단계

#### `src/dataset.py` [2] · 164줄

실험에 쓸 **q와 K를 준비**합니다.

```python
snapshot_from_config(cfg, seq_len=2048)
#   ① cache/tensors/ 에 실제 Llama 텐서가 있으면 그걸 쓴다
#   ② 없으면 합성 데이터를 만든다
```

합성도 그냥 랜덤이 아닙니다. 실제 어텐션의 성질을 흉내 냅니다.

```
   sink_frac 0.04       토큰 4% 를 "sink" 로 만들어 점수를 높게
   target_logit_std 2.5 로짓 표준편차를 실제와 맞춘다
```

**왜 필요하냐** — 표준정규로 그냥 만들면 softmax가 거의 균등해져 **종단이 비현실적으로 안 일어납니다.**

#### `src/model_hooks.py` [2] · 187줄

**진짜 Llama에서 q, K를 뽑습니다.**

```python
capture_qk(hf_id, text, layer_idx=8, head_idx=0, apply_rope=True)
```

두 가지가 중요합니다.

**GQA 인덱싱** — Llama 3.2 1B은 질의 헤드 32개, KV 헤드 8개. 4개가 하나를 공유합니다.

```python
kv_head = head_idx % n_kv_heads                # 기존 (틀림)
kv_head = head_idx // (n_heads // n_kv_heads)  # 수정
```

기존 코드는 **32개 중 0번과 31번만 우연히 맞았습니다.** 기본값이 0이라 아무도 몰랐습니다.

**RoPE** — 적용한 **뒤**를 잡아야 합니다. 안 그러면 `Q±` 폭이 12% 달라집니다.

#### `src/seeding.py` [2] · 29줄

재현용 RNG. 아날로그 설계 잔재를 정리해 `data_rng` 하나만 남겼습니다.

#### `src/config.py` [2] · 360줄

`config/*.yaml`을 읽습니다. 그냥 읽는 게 아니라 **틀린 설정을 잡습니다.**

```
   required 항목 누락       →  예외
   source 가 estimate       →  경고 (논문에 쓰면 안 됨)
   yaml 들여쓰기에 탭       →  예외 (자체 파서가 조용히 다른 구조를 만든다)
   절 이름 오타             →  유사 키를 알려 준다 (difflib)
```

**`read_fields`가 핵심입니다** — 설정과 코드를 잇는 배선표입니다. 새 파라미터를 넣으면 여기 등록해야 연결됩니다. 안 하면 **기본값으로 조용히 돌아갑니다.**

### 4-2. 팀 1 — 숫자를 만든다

#### `src/quantize.py` [1] · 187줄

```python
key    = quantize_key(k_real)         # 실수 K → INT8 (uint8 저장 + zero_point)
planes = to_bitplanes(key.stored)     # → (8, 토큰, 차원)
```

**★ 인계 1** 지점입니다. `index 0 = MSB`가 규약입니다.

**성립 조건을 입구에서 막습니다.**

```
   0 ≤ K < 2^n_planes
```

벗어나면 변환이 **조용히 잘라내서** 평면이 K를 대표하지 못하고, 그 순간 상한식이 깨집니다. `K=256`을 넣으면 복원값이 0이 됩니다.

#### `src/accumulator.py` [1] · 163줄

두 가지 일을 합니다.

**① K의 스케일을 q에 접는다**

```python
fq = fold_and_quantize_query(q_real, key)
#   내적 안에서 K의 scale 을 없앤다 → 정수끼리만 곱하면 된다
#   zp_correction 도 함께 (스텝당 상수 하나)
```

**② 시프트 누산**

```python
accumulate(P)     #  S ← (S << 1) + P_b   를 8번
```

원래 `tensordot`(곱셈)이었는데 팀 1이 **실제 시프트-가산 루프**로 바꿨습니다. **"곱셈기 없음" 주장의 코드 근거**입니다.

#### `src/masked_sum.py` [1] · 205줄

```python
P = partial_dots(q_stored, planes)    # (8, 스텝, 토큰)
```

**★ 인계 2** 지점입니다.

**규약을 코드로 막습니다** — 비트가 0/1이 아니면 예외입니다. einsum은 곱해 버리고(2→2q) 선택-덧셈은 버리는데(2→0), **둘 다 조용해서** 여기서 안 막으면 못 찾습니다.

> **이 텐서를 한 번만 만들고 모든 스윕이 재사용합니다.** margin·θ정책·top-k를 바꿔도 `P_b`는 그대로라서요. 실험이 빠른 이유입니다.

#### `src/memory.py` [1] · 247줄

**★ 인계 3**을 받아 처리합니다.

```python
account_step(read_live, bram)
#  {'reads_ideal': 654,    ← 이론 (살아있는 쌍의 수)
#   'words_bram':  654,    ← 실현 (BRAM 워드 단위)
#   'read_saving_bram': 0.19}
```

**두 값을 반드시 함께 냅니다.** 논문에 `ideal`만 쓰면 발표에서 바로 지적당합니다.

```
   word_reads_scattered()   워드폭 함정 계산 — 흩어져 있으면 워드 전체를 읽는다
   latency_planes()         판정 지연을 평면 수로 환산 (팀2 추가)
   memory_cycles = ceil(words_bram / n_ports)
```

**이 모델이 우리 `n_ports` 오류를 드러냈습니다** (§6-1).

#### `src/schedule.py` [1] · 245줄

죽은 자리를 어떻게 메울지 3정책입니다.

| 정책 | 방식 | 실현 절감 |
|---|---|---|
| `batch` | 묶음 단위로만 건너뜀 | 3.4% |
| `compaction` | 매 평면 앞으로 당김 | **27.9%** ✓ |
| `two_phase` | M0까지 스캔 후 한 번 압축 | 중간 |

`baseline_cycles`가 기준 설계(①)의 사이클을, `apply`가 총 사이클과 `mem_overlap`을 계산합니다.

### 4-3. 팀 2 — 언제 그만둘지 정한다

#### `src/bounds.py` [2] · 119줄

```python
b = step_bounds(q_stored)     # Q+ = 양수 합,  Q− = 음수 합
b.l_offset(m)                 # W_m × Q−   (하한 오프셋)
b.r(m)                        # W_m × Q+   (상한 오프셋)
b.width(m)                    # U − L      (불확실성 폭)
```

`verify_bracket()`이 `L ≤ 참값 ≤ U`를 실제로 검사합니다 — **1,678만 건** 돌린 게 이 함수입니다.

#### `src/threshold.py` [2] · 184줄

```python
kth_largest(values, live, k)   #  θ = 활성 하한 중 k번째로 큰 값
```

**`ThetaPolicy`가 4정책을 담습니다.**

| 정책 | 무엇 | 무손실? |
|---|---|---|
| `every_plane` | 평면마다 다시 계산 | ✓ **채택** |
| `once_at_m` | m번째 평면에서 한 번만 | ✓ |
| `prev_step` | 직전 스텝 θ 재사용 | **✗ 손실** |
| `oracle_fixed` | 정답을 미리 아는 이상적 경우 | 비교용 |

**`prev_step`이 손실인 걸 우리가 찾았습니다.** 문헌 어디에도 스텝 간 θ 재사용이 없어 비교 대상이 없었습니다.

`margin_abs`가 4가지 정규화 단위를 담습니다.

`topk_indices`는 `-inf`를 걸러냅니다 — 종단된 토큰이 top-k를 채워 보존율이 부풀려지던 문제입니다. **양 팀이 독립적으로 같은 수정에 도달**한 부분입니다.

#### `src/terminator.py` [2] · 467줄 ★★ 심장

```python
run_step(partials, bounds, policy, decision_latency=1)
```

**평면 루프를 돌며** 이 일을 합니다.

```
   각 평면 t 에서:
     ① read_live[t] = live[t − latency]      실제로 읽는 토큰 (지연 반영)
     ② S += W[t] × P[t]                      누산 (죽은 건 동결)
     ③ L = S + l_offset,  U = S + r          구간
     ④ θ = k번째 하한
     ⑤ U < θ + margin  →  죽인다
     ⑥ 살아남은 게 k개 미만이면 → 상한 큰 순으로 되살린다 (가드)
```

> **`read_live`와 `alive`가 다릅니다.**
> `alive`는 논리적 생존, `read_live`는 **판정 지연을 반영한 실제 읽기**입니다.
> 이 구분을 안 하면 절감이 과대평가됩니다.

**`StepResult`가 내보내는 것**

```
   s_int        확정 점수 (종단된 건 그 시점의 부분 점수)
   alive        생존 마스크
   term_plane   토큰별 종단 평면
   read_live    ★ 인계 3 — 팀 1이 회계에 쓴다
```

**2026-08-30 추가 — 팀원의 v2 채택분**

```python
ctrl = TerminationController(bounds, policy, n_tokens)
while not ctrl.done:
    dec = ctrl.process_plane(P[ctrl.next_plane])   # PlaneDecision
```

**평면을 하나씩 넣는 경로**입니다. `PlaneDecision`이 평면마다 `read_mask`·`lower`·`upper`·`theta`·`killed`를 전부 내줍니다. **RTL 파형과 평면 단위로 대조**하려고 넣었습니다.

`run_step_from_frontend(q_stored, partials)`는 **팀1→팀2 인계를 한 함수로** 만든 것입니다.

#### `src/output_stage.py` [2] · 215줄

```python
emit_step(result, spec, zp_correction=...)
#   생존 집합을 (번호, 점수) 스트림으로
```

```
   score_idx    [10:0]           토큰 번호
   score_data   [23:0] signed    zero-point 보정 후 정수 점수
   score_valid                   1이면 이번 사이클 값이 유효
   score_last                    마지막 토큰에서 1   ← 개수가 가변이라 필요
```

**zero-point 보정을 여기서 한 번만** 합니다. 모든 토큰에 같은 상수라 **순위가 안 바뀌니까**, 판정은 정수로 하고 보정은 마지막에 24b 감산기 하나로 끝냅니다.

**비트폭 초과를 예외로 막습니다** — 골든모델은 int64라 그냥 담기지만 하드웨어는 안 됩니다.

### 4-4. 통합·비교

#### `src/designs.py` [2] · 162줄

**4설계를 같은 코드로 돌립니다.**

| # | 이름 | 내용 | 역할 |
|---|---|---|---|
| ① | `baseline` | 병렬 INT8 MAC | 기존 방식 |
| ② | `seq` | 비트평면, 종단 없음 | **종단의 순수 이득을 재는 기준** |
| ③ | `exact` | 비트평면 + 무손실 종단 | 제안 |
| ④ | `approx` | ③ + margin | 품질 조금 내주고 더 아낌 |

**②가 중요합니다.** ①과 ③을 바로 비교하면 **비트평면의 대가**와 **종단의 이득**이 섞입니다.

#### `src/decode_loop.py` [2] · 417줄

**디코드 루프 전체**를 돕니다. `T`가 자라는 걸 반영합니다.

```python
run_decode(wb, design="exact", top_k=16, margin=0.40)
```

```
   스텝마다 판정 지연 재계산          (auto 모드)
   정확도 두 종류를 분리해 누적        종단 손실 vs top-k 자체의 손실
   토큰별 종단 평면 히스토그램
   출력 단계 회계 (생존 수, CNT_TRUNC)
   사이클·메모리 누적
```

**`DecodeWorkbench`가 부분내적을 한 번만 만들고** 모든 스윕이 재사용합니다.

### 4-5. 실험 9개

| 실험 | 묻는 것 | 소유 | 시간 |
|---|---|---|---|
| `exp1` | 종단이 실제로 일어나는가, 어느 평면부터인가 | 팀 2 | 41s |
| `exp2` | margin을 얼마까지 밀 수 있나 | 팀 2 | 326s |
| `exp3` | θ를 언제 확정할 것인가 | 팀 2 | 202s |
| `exp4` | 스케줄 정책 × BRAM 워드폭 | 팀 1 | **1,348s** |
| `exp5` | 문맥 길이와 상위 k 스캔 | 팀 1 | 33s |
| `exp6` | Fmax 저하를 넘는 손익분기 (**연산축**) | 팀 1 | 7s |
| `exp7` | 병목 위치가 종단의 가치를 정한다 (**메모리 포함 축**) | 팀 2 | 278s |
| `exp8` | **실측 perplexity** (보간 아님) | 팀 2 | 매우 김 |
| `exp9` | **margin 커버리지** — 전 헤드 × 여러 텍스트 | 팀 2 | 매우 김 |

> `exp6`과 `exp7`은 **같은 질문을 다른 축에서** 봅니다. `exp6`은 연산 사이클만 세므로 ① 대비로는 절대 안 이깁니다. `exp7`은 메모리를 포함해 재고, 거기서 T=2048이면 **이깁니다.**

### 4-6. 설정 파일

| 파일 | 무엇 |
|---|---|
| `config/model.yaml` | 모델 스펙, `seq_len`(= N_MAX), 캡처할 층/헤드, 합성 파라미터 |
| `config/quant.yaml` | 평면 수, **`margin.value`**, 양자화 방식 |
| `config/hardware.yaml` | **RTL 팀과의 인터페이스.** lanes, word_tokens, n_bram_ports, 자원/Fmax |
| `config/sweeps.yaml` | 실험별 스윕 범위 |

**숫자는 전부 여기 있습니다. 코드를 고치지 마세요.**

> 더 깊이: 이 절 §5-3(인계 자료형)·§5-7(중간 산출물), `docs/structure.md` (저장소 구조)

---

## 5. 분업과 인계 — 두 팀의 경계

### 5-1. 한 줄 요약

```
   팀 1 = 비트평면 BRAM + 부분내적 누산      "숫자를 만든다"      5개 1,047줄
   팀 2 = Q 레지스터 + 종단판정 + θ + 출력   "언제 그만둘지 정한다" 10개 2,304줄
```

두 팀은 **세 지점에서만** 데이터를 주고받습니다. 그 외엔 서로의 내부를 모릅니다.

### 5-2. 인계 3지점 — 실제 자료형

`T=256`, `D=64`, 평면 8 기준으로 **실제 측정한 값**입니다.

```
   단계                          무엇                shape          dtype
   ──────────────────────────────────────────────────────────────────────
   입력  dataset [2]            QKSnapshot.q       (256, 64)      float64
                                QKSnapshot.k       (256, 64)      float64

   quantize.quantize_key [1]    KeyQuant.stored    (256, 64)      uint8
   quantize.to_bitplanes [1]    비트평면            (8, 256, 64)   uint8    ★ 인계 1
   accumulator.fold_… [1]       FoldedQuery.stored (256, 64)      int16
                                zp_correction      (256,)         int64
   masked_sum.partial_dots [1]  P_b                (8, 256, 256)  int32    ★ 인계 2

   bounds.batch_step_bounds [2] Q+ / Q−            (256,)         int64
   accumulator.cumulative [1]   S_m (모든 m)        (9, 1, 256)    int64

   terminator.run_step [2]      alive              (101,)         bool     ★ 되먹임
                                read_live          (8, 101)       bool     ★ 인계 3
                                s_int              (101,)         int64

   output_stage.emit_step [2]   score_idx          (22,)          int64    ★ 최종
                                score_data         (22,)          int64
```

#### 인계 1 — 비트평면 `(8, T, D) uint8`

팀 1의 `to_bitplanes`가 냅니다. **index 0이 MSB.**

**규약**: 값은 반드시 0 또는 1. **성립 조건**: `0 ≤ K < 2^n_planes`.

#### 인계 2 — 부분내적 `P_b` `(평면, 스텝, 토큰) int32`

팀 1의 `partial_dots`가 냅니다.

**인계를 한 함수로 부를 수 있습니다.**

```python
from src.terminator import run_step_from_frontend
run_step_from_frontend(q_stored, partials)      # Q+/Q− 를 내부에서 한 번만 계산
```

기존엔 호출자가 `StepBounds`를 직접 만들어야 해서 **팀 1이 팀 2 내부를 알아야** 했습니다.

#### 인계 3 — `read_live` `(평면, 토큰) bool` ★ 되먹임

**팀 2가 만들어 팀 1이 받습니다.** 방향이 반대입니다.

```
   read_live[t][j] = 평면 t 를 읽을 때 토큰 j 를 실제로 읽는가
```

**이게 이 프로젝트의 심장입니다.** 팀 2의 판정이 팀 1의 읽기를 막고, 그 절감이 성능이 됩니다.

### 5-3. 팀 1 코드 중 우리가 가져다 쓴 것

| 가져온 것 | 파일 | 우리에게 어떤 영향 |
|---|---|---|
| **메모리 사이클 모델** | `memory.py` `schedule.py` | **우리 `n_ports` 오류를 드러냈다** — 메모리가 연산의 11배 |
| **`mem_overlap`** | `schedule.py` | 시간 축의 정의. `false`면 결론이 거의 2배 나빠진다 |
| **`batch` 정책 정정** | `schedule.py` | 우리 워드폭 표가 틀렸음이 드러났다 |
| **기준선 사이클 버그 수정** | `designs.py` | ①이 자기보다 8배 느리다고 나오던 것 |
| **`read_fields` 배선** | `config.py` | 우리 새 필드는 전부 여기 등록해야 한다 |
| **`quantize` 범위 검사** | `quantize.py` | 우리 수정의 **상위집합** |
| **`accumulate` 시프트 누산** | `accumulator.py` | "곱셈기 없음" 주장의 코드 근거 |

### 5-4. 병합 시 충돌 5곳과 해결

| 파일 | 채택 |
|---|---|
| `quantize.py` | 범위검사 **팀 1**(상위집합) + 평면 0 예외 문구 **팀 2** |
| `threshold.py` | **양 팀이 독립적으로 같은 수정에 도달.** 코드 팀 1, 실측 근거 팀 2 |
| `memory.py` | 기본값 팀 1 유지 + 팀 2 `latency_planes` |
| `schedule.py` | 팀 1 배선에 팀 2 필드 2개 추가 |
| `config/quant.yaml` | 팀 2 |

### 5-5. 서로의 코드가 상대의 오류를 드러냈다

**① 팀 1의 메모리 모델이 우리 `n_ports` 오류를 잡았다**

`WORD_TOKENS=1`로 정하면서 포트는 2로 뒀는데, **메모리가 연산의 11배**가 됐습니다. → §6-1

**② 팀 1의 `batch` 정정이 우리 워드폭 표를 뒤집었다**

기존 표의 `batch` 열이 **사실은 `two_phase` 값**이었습니다.

**③ 우리 실험이 팀 1 테스트의 전제를 낡게 만들었다**

팀 1 테스트 2개가 `terminator`로 들어오는 간선 **목록을 통째로** 못 박고 있어, `exp8`·`exp9`를 추가하자 깨졌습니다. **`src/` 안의 불변식만** 고정하도록 일반화했습니다.

### 5-6. 팀원의 `terminator_v2` 채택 (2026-08-30)

동작이 **완전히 같은 것**을 확인하고 채택했습니다.

```
   등가성 5,076건 불일치 0     (무작위 5,000 / 경계 64조합 / 극단 12조합)
   우리 원본 155줄  →  채택본 467줄     테스트 416 → 424     성능 1.17배
```

**가져온 것 3가지**

1. `TerminationController` — 평면별 관측. **RTL 파형 대조에 필요**
2. `run_step_from_frontend` — 팀1→팀2 인계를 한 함수로
3. **평면수 불일치 차단** — `partials`가 더 적을 때 우리 원본은 예외 없이 통과해 **조용히 틀린 답**을 냈다

**고친 것 1가지** — `run_step_from_frontend`가 `top_k=8`·`once_at_m=3`·`margin_mode='relative_gap'`을 자체 기본값으로 들고 있었습니다. 셋 다 우리 확정값의 **옛 버전**이라 설정을 우회하고 있었습니다.

### 5-7. 단계별 중간 산출물 — 무엇이 논문에 쓰이나

| 산출물 | 만드는 곳 | 무엇을 위한 것 | 논문에 쓰나 |
|---|---|---|---|
| `KeyQuant` | [1] | INT8 표현 + zero_point | — |
| **비트평면** | [1] | 인계 1. 메모리 배치의 근거 | 구조도 |
| `FoldedQuery` | [1] | scale을 q에 접어 곱셈 제거 | "곱셈기 없음" 논거 |
| **`P_b`** | [1] | 인계 2. 모든 스윕의 원재료 | — |
| `Q+` / `Q−` | [2] | 상하한의 상수. 스텝당 1회 | 하드웨어 사양 |
| `S_m`, `L_m`, `U_m` | [1]/[2] | 구간. **수렴하는 것이 핵심 그림** | 도해 |
| `θ` | [2] | 기준선. **무손실 증명의 축** | 알고리즘 |
| **`alive`** | [2] | 생존 집합 | 품질 평가 |
| `PlaneDecision` | [2] | **평면별 중간값.** RTL 파형 대조용 | 검증 |
| **`read_live`** | [2] | 인계 3. **절감이 발생하는 지점** | **핵심 측정값** |
| `ReadAccount` | [1] | 이론 vs 실현 절감 | **표** |
| `ScheduleResult` | [1] | 연산·메모리 사이클 | **표** |

### 5-8. 넘길 때

```bash
git apply team2_code/onto_team1.patch
PYTHONUTF8=1 python tests/run_tests.py        # 424 통과
```

깨끗한 팀 1 트리에서 적용 → 424 통과를 확인했습니다.

> 더 깊이: `docs/ko/terminator_v2_review_ko.md` (v2 검토 회신)

---

## 6. 실전 — 3가지 함정

**여기가 이 프로젝트에서 실제로 어려운 부분입니다.** 교과서엔 안 나옵니다.

### 6-1. 함정 ① — 안 읽는다고 안 읽히는 게 아니다

BRAM은 **워드 단위**로 읽습니다. 워드 하나에 여러 토큰이 묶여 있으면,

> **그 안에 하나라도 살아 있으면 워드 전체를 읽어야 합니다.**

죽은 토큰이 흩어져 있으면 **절감이 실현되지 않습니다.**

```
   이론 절감 27.9% 가 실제로는  (T=512 합성 기준)

   워드폭   batch          compaction
      1    3.4% (12%)     27.9% (100%)   ← 채택
     32    3.8% (14%)     26.6%  (95%)
```

**`batch` 열이 평평합니다.** 워드폭을 아무리 줄여도 3.4~3.8%입니다 — 묶음(32)이 건너뛰기 단위를 정하므로 워드폭이 개입할 여지가 없습니다.

> **실현 절감은 워드폭과 스케줄 정책이 함께 정합니다.** 둘 중 하나만 골라선 안 됩니다.

**`WORD_TOKENS = 1` 채택 이유** — 압축 회로가 필요 없습니다. 생존 토큰 목록으로 주소를 만들면 BRAM이 **이미 정렬된 데이터를 내주므로**, work-compaction이 주소 생성에 흡수됩니다.

#### ★ 그런데 `n_ports`도 함께 움직여야 합니다

`WORD_TOKENS=1`로 정하면서 `n_bram_ports`는 2로 뒀더니:

```
   word_tokens=1, n_ports=2    메모리 376,650  vs  연산 33,043    11배 병목
   word_tokens=1, n_ports=32   메모리  23,761  vs  연산 33,043    연산 병목
```

**조건은 하나입니다.**

```
   word_tokens × n_ports ≥ lanes
```

한 사이클에 `lanes`개 토큰을 먹이려면 `lanes × D` 비트가 필요합니다. 워드가 좁아진 만큼 포트가 늘어야 대역폭이 유지됩니다.

### 6-2. 함정 ② — 판정이 늦게 도착한다

평면 `m`의 판정이 나오려면 가산 트리를 거쳐야 하는데 **8사이클** 걸립니다. 그동안 다음 평면 읽기는 **이미 나가 있습니다.**

```
   평면0 읽기 ██████
              └─ 가산트리 ─ 비교 ─ 판정!    ← 8사이클 뒤
   평면1 읽기       ██████        ↑
                                 이미 읽어버렸다
```

#### 핵심 — 8사이클이 "몇 평면"인지는 `T`가 정한다

평면 1장 처리에 `ceil(T / LANES)` 사이클이 걸립니다.

```
   판정 지연(평면) = ceil( 8 / ceil(T / 32) )

    T      평면당 사이클    지연(평면)
   32          1              8      ← 종단 이득이 0이 된다
  128          4              2
  256          8              1
  512         16              1
```

**토큰이 많으면 평면 하나가 오래 걸려 지연이 상대적으로 작아집니다.**

디코드는 `T`가 1에서 2048로 **자랍니다.** 고정값 1을 쓰면 초반 스텝을 전부 틀리게 셉니다.

```
    T     fixed      auto      차이
  128     14.9%      4.0%   −10.8%p
  256     19.4%     11.4%    −8.0%p
  512     23.3%     21.4%    −1.9%p
```

**짧은 문맥에서 절감이 거의 사라지지만 이게 사실입니다.** 부풀린 숫자를 버리고 **"긴 문맥용 설계"라는 논지를 정량화**했습니다.

> **RTL 지침** — `decision_latency_planes`를 `parameter`로 박지 말고 `n_token`과 `LANES`로부터 **런타임에 계산**할 것. 상수로 두면 짧은 문맥에서 **무손실이 깨집니다.**

### 6-3. 함정 ③ — 아껴도 시간이 안 줄 수 있다

전체 시간은 이렇게 정해집니다.

```
   memory_cycles = ceil(words_bram / n_ports)
   total = max(compute, memory)              (읽기와 연산이 겹칠 때)
```

**종단이 줄이는 건 메모리**입니다. `max()`가 연산을 집으면 **메모리를 아무리 아껴도 시간이 그대로**입니다.

```
   포트 │   T=512        T=1024       T=2048
        │ 시간   병목   │ 시간   병목  │ 시간   병목
     16 │ .760 메모리   │ .713 메모리 │ .668 메모리
     24 │ .804  연산    │ .723 메모리 │ .671 메모리
     32 │ .990  연산    │ .836  연산  │ .733  연산

   교차점    T=512: 24 포트     T≥1024: 32 포트
```

**읽기 절감은 포트와 무관합니다** — 종단 로직이 같으니 당연합니다. **시간만 움직입니다.**

> **문맥이 길수록 교차점이 올라갑니다.** 메모리 병목 구간이 넓어져 종단이 더 오래 값어치를 합니다. **이것이 긴 문맥을 노리는 이유의 정량적 근거입니다.**

**`N_MAX=2048`이면 포트 32로도 종단이 값어치를 합니다** (0.733). 생존이 4.3%라 compaction이 **연산 사이클도 26.7% 줄이기** 때문입니다.

### 6-4. 그래서 먼저 확인한 것 — 기준선도 메모리 병목이다

"비트평면 순차는 ①보다 8배 느리다"는 **연산만 센 값**입니다. 기준 설계도 K를 전부 읽어야 하고, 그쪽이 오히려 병목입니다.

```
   설계        연산      메모리     합계        (실제 Llama T=2048)
   baseline   66,465   524,664   524,664     ← ① 도 메모리 병목
   seq       531,720   524,664   531,720
   exact     389,673   350,506   389,673
   approx    325,382   286,554   325,382

   ③/① 합계 = 0.743      ← 26% 빠르다
```

---

## 7. 최종 결과물

### 7-1. ★ 제안 설계가 기준선을 이긴다

실제 Llama 3.2 1B 텐서(L8/H0, RoPE, wikitext-2), Fmax 저하 `165/200 = 0.825` 반영.

```
     T     생존     읽기절감    exact vs①   approx vs①
   512    11.3%      24.6%      0.793        0.861
   768     8.6%      27.2%      0.893        0.990
  1024     7.1%      29.0%      0.961        1.081
  1280     6.0%      30.2%     ★1.009        1.145      ← exact 손익분기
  1536     5.3%      32.0%      1.058        1.213
  2048     4.3%      33.3%     ★1.111        1.290
```

**512에서 0.79로 지던 것이 2048에서 1.11로 이깁니다.**

**원인은 하나** — 문맥이 길수록 생존 비율이 **11.3% → 4.3%**로 떨어집니다. 어텐션이 희소해지므로 종단이 연산과 메모리를 **함께** 줄입니다. 기준선은 `T`에 선형으로 늘 뿐 아무것도 못 아낍니다.

### 7-2. 종단이 실제로 일어난다 (exp1)

실제 텐서 T=2048, `K_TOP=16`:

```
   평면    토큰수         비율
     0~2        0        0.0%     구간이 너무 넓다
     3    237,362       11.3%     ← 종단 시작
     4  1,196,809       57.1%     ← 최빈
     5    472,977       22.6%
     6    113,745        5.4%
     7     31,339        1.5%
     8     43,400        2.1%     끝까지 생존
   ────────────────────────────
   97.9% 가 마지막 평면 전에 종단.  평균 종단 평면 4.349
```

**`M0 = 4` 결정이 실제 데이터로 확인됩니다.**

### 7-3. `K_TOP = 16` — 실측 perplexity (exp8)

**전 층(16개) 동시 교체** 기준입니다.

```
    k   방식              ppl    full 대비   평균 생존   보간 추정(옛값)
    8   오라클 top-k    8.3313    +9.13%       7.9
    8   우리 (정확)     8.0996    +6.09%      10.0  ★      +11.9%
   16   오라클 top-k    7.9114    +3.63%      15.8
   16   우리 (정확)     7.8464    +2.78%      19.5  ★       +5.8%   ← 채택
   16   우리 (m=0.70)   7.8967    +3.43%      16.8  ★
   32   오라클 top-k    7.7491    +1.50%      31.0
   32   우리 (정확)     7.7123    +1.02%      37.3  ★       +2.7%

   full attention ppl = 7.6345     ★ = 오라클 하드 top-k 보다 좋다 (6/6)
```

**8 → 16이 knee입니다.**

```
    8 → 16    +6.09% → +2.78%    품질 3.31%p 개선
   16 → 32    +2.78% → +1.02%    품질 1.76%p 개선
```

**6/6 전부 오라클보다 좋습니다.** 생존 집합이 top-k의 **상위집합**이라 "버릴 수 없는 토큰"이 함께 남기 때문입니다.

> **옛 근거는 보간값이었습니다.** 하드 top-k 곡선에 생존 토큰 수를 로그 보간해 읽었는데, 상위집합 이득을 담을 수 없어 **모든 k에서 실제의 약 2배로 나빴습니다.** 우리에게 불리한 추정이었습니다.

### 7-4. `MARGIN = 0.40` — 384조합에서 수렴 (exp9)

표본을 넓히며 추적했습니다.

```
   조합 수                              전역 안전 margin   헤드별 이득
     8  (2텍스트 × 2층 ×  2헤드)             0.70           +2.9%p
    32  (1텍스트 × 4층 ×  8헤드)             0.50           +8.0%p
    96  (3텍스트 × 4층 ×  8헤드)             0.40          +11.1%p
   384  (3텍스트 × 4층 × 32헤드)             0.40          +10.7%p   ← 수렴
```

**"전역 안전값"은 전 조합의 최솟값**이라 표본이 커지면 내려갑니다. **96조합에서 0.40이 나온 뒤 헤드를 4배로 늘려도 그대로입니다.**

```
   margin   절감 평균   절감 최소   보존 최악    실질
     0.00     26.5%      17.6%    1.00000    정확 모드
     0.40     34.5%      25.1%    1.00000    완전 무손실     ← 기본값
     0.50     37.5%      27.9%    0.99987    10만개 중 13개
     0.70     46.6%      36.7%    0.99505    1000개 중 5개   ← 운영값
     0.80     54.4%      46.2%    0.93398    여기서 무너진다
```

조합별 knee는 **0.40 ~ 0.80, 중앙값 0.70**입니다. 0.7은 대부분의 헤드에서 맞고 **이상치가 전역값을 끌어내립니다.**

**`MARGIN`은 R/W 레지스터라 하나를 고를 필요가 없습니다** — 기본 0.40(무손실 보장), 운영 0.70(99.5%).

가장 취약한 곳은 **깊은 층(L10, L15)** 이었습니다. 어텐션이 평평해져 상위 몇 개가 확실히 앞서지 못합니다.

### 7-5. 무손실 검증

```
   상한식 L_m ≤ s ≤ U_m     16,785,408건 검사,  위반 0건    (실제 Llama T=2048)
   top-16 보존율            T = 512 / 1024 / 2048 전부 1.00000
   384조합                  margin 0.40 까지 보존율 1.00000
```

### 7-6. 검증 현황

```
   테스트           424개 통과 / 0 실패
   crosscheck       12/12 매칭
   돌연변이 시험     14종 중 13종 검출  (남은 1종은 값이 안 바뀌는 무의미한 변형)
   실험             exp1 ~ exp9 전부 정상
```

**돌연변이 시험**은 코드를 일부러 망가뜨리고 어느 테스트가 잡는지 세는 것입니다. **통과만으로는 근거가 안 됩니다.**

```
M1  bounds  r(m) 에서 −1 제거          bounds, quantize, terminator, threshold
M2  bounds  l_offset 부호 뒤집기        bounds, decode_loop, designs, terminator
M3  bounds  q_pos/q_neg 자리 바꾸기     bounds, terminator
M4  thresh  kth_largest −k → k−1       decode_loop, designs, terminator, threshold
M5  thresh  margin 음수 클램프 제거      ★ threshold 만
M6  term    판정 지연 무시              ★ terminator 만
M7  term    keep-top-k 가드 제거        designs, terminator
M8  term    upper < theta → <=         ★ terminator 만
M9  term    frozen 값을 0 으로          ★ 새 테스트만   ← 두 버전 공통 구멍이었다
M10 term    frozen 갱신 생략            ★ 새 테스트만
M11 term    평면수 검증 제거             ★ 새 테스트만   ← v2 가 막아 준다
M12 term    frontend 기본값 되돌림       ★ 새 테스트만
M13 term    PlaneDecision read_mask 오류 ★ 새 테스트만
M14 term    live_after 오류             ★ 새 테스트만
```

> **동률을 일부러 만들어 메운 구멍.** `U_m < θ`를 `<=`로 바꿔도 무작위 데이터에선 **동률이 안 나와** 검출이 불가능했습니다. `q_pos=64`일 때 `R_2 = 63×64 = 4032`가 `S_2`와 정확히 같아지는 구성을 만들어 메웠습니다.

### 7-7. RTL 팀에 넘기는 것

```
   config/hardware.yaml     파라미터 + 자원/Fmax 추정치 (source: estimate 8개)
   src/output_stage.py      score_out 블록의 참조 구현
   src/terminator.py        TerminationController 로 평면별 골든 벡터 생성
   rtl_data/mock/*.csv      crosscheck 대조용 예측값
   ARCHITECTURE.md          비트폭 · 상태기계 · 레지스터 맵
```

---

## 8. 직접 돌려보기

### 8-1. 필수 2가지

```bash
export PYTHONUTF8=1        # PowerShell: $env:PYTHONUTF8 = "1"
export HF_HUB_OFFLINE=1    # 모델이 이미 있으면
```

**Windows 기본 코드페이지가 cp949라 `PYTHONUTF8=1`을 빼면 한글이 `����`로 깨집니다.**

### 8-2. 제일 먼저

```bash
PYTHONUTF8=1 python tests/run_tests.py
```

```
통과 424  실패 0  건너뜀 0        ← 이게 나와야 한다
```

**실패가 하나라도 있으면 다른 걸 돌리지 마세요.** 수치를 믿을 수 없습니다.

필요한 것: **Python 3.10+ 와 NumPy**. PyYAML은 없어도 됩니다(자체 파서로 대체).

### 8-3. 실험

```bash
PYTHONUTF8=1 python run_paper_experiments.py --no-figures      # 전체 (40분+)
PYTHONUTF8=1 python run_paper_experiments.py --only exp2       # 하나만
PYTHONUTF8=1 python -m experiments.exp2_margin_sweep           # 이렇게도 된다
```

결과는 `outputs/raw/exp<N>_<이름>.csv`입니다.

> `--quick`은 `seq_len`을 128로 낮춥니다. **논문 수치로 쓰면 안 됩니다** — 짧은 문맥은 판정 지연 때문에 절감이 반토막입니다.

### 8-4. 실제 텐서로 돌리려면

기본은 합성 데이터입니다. **먼저 캡처**해야 합니다.

```bash
PYTHONUTF8=1 HF_HUB_OFFLINE=1 python -m src.model_hooks \
    --seq-len 2048 --layer 8 --head 0 --rope \
    --text-file cache/wikitext2_test.txt
```

`--rope`를 **꼭 주세요.** 빼면 `Q±` 폭이 12% 달라집니다.

**잡혔는지 확인:**

```bash
PYTHONUTF8=1 python -c "
from src.config import load_config
from src.dataset import snapshot_from_config
print(snapshot_from_config(load_config(), seq_len=2048).source)"
```

```
unsloth/Llama-3.2-1B:L8:H0:rope    ← 실제 텐서
synthetic                           ← 합성. 캡처가 안 잡힌 것
```

### 8-5. 설정을 바꾼 뒤 반드시

```bash
PYTHONUTF8=1 python tests/run_tests.py                         # 424 통과
PYTHONUTF8=1 python run_paper_experiments.py --generate-mock   # mock 재생성
PYTHONUTF8=1 python run_paper_experiments.py --crosscheck      # 12/12
```

**mock 재생성을 빼먹으면** crosscheck가 예전 설정과 비교해 불일치가 납니다.

### 8-6. 자주 나는 문제

| 증상 | 원인 | 해결 |
|---|---|---|
| 한글이 `����` | cp949 | `PYTHONUTF8=1` |
| `출처: synthetic` | 캡처 파일명 불일치 | §8-4 |
| crosscheck 불일치 | mock이 옛 설정 | `--generate-mock` |
| `score_idx N이 9b를 넘는다` | `N_MAX` 올리고 `idx_bits` 안 올림 | `hardware.yaml` |
| **로딩 중 Segmentation fault** | **RAM 부족** (Llama fp32 ≈ 5 GB) | 모델 쓰는 실험을 **동시에 돌리지 말 것** |
| exp4가 안 끝남 | T=2048에서 300조합 | `sweeps.yaml`에서 축을 줄이거나 `--quick` |

### 8-7. 논문 수치를 쓸 때

| 쓸 것 | 쓰지 말 것 |
|---|---|
| `read_saving_bram` | `read_saving_ideal` (실현 안 됨) |
| `total_cycles_with_memory` | `total_cycles` (연산축만) |
| exp8 실측 perplexity | 보간값 +5.8% |
| 기본 실행 | `--quick` |

`source: estimate` 8개는 **Vivado 실측으로 교체할 자리**입니다 — 실행할 때마다 몇 개인지 경고가 뜹니다.

> 더 깊이: `실험_실행법.md` (준비 · 실행 · 설정 · 결과읽기 · 문제해결 10절)

---

## 9. 남은 일과 정직하게 짚을 것

### 9-1. 아직 안 정해진 것

전부 **RTL 실측 / Vivado 합성**이 있어야 정해집니다.

| 항목 | 왜 미결정인가 |
|---|---|
| **`mem_overlap`** | 지금 `true`. `false`면 합계가 `compute + memory`라 **거의 2배** |
| **`source: estimate` 8개** | 자원·Fmax 추정치. **논문에 그대로 쓰면 안 된다** |
| **헤드별 `MARGIN` 테이블** | 이득 **+10.7%p**, 면적은 32엔트리×8b = **256비트**로 사실상 0. 진짜 비용은 **모델마다 헤드별 knee를 측정해 채우는 배포 부담** |
| **`N_PORTS` 최종값** | 채택 32. `N_MAX=2048`이면 32로도 종단이 값어치를 한다(0.733) |

### 9-2. 팀 1이 정해야 하는 것

| 항목 | 내용 |
|---|---|
| `exp4`·`exp6`의 `top_k: 8` | `K_TOP=16` 확정과 어긋납니다 |
| `exp6`의 축 | `total_cycles`(연산축)만 봅니다. 우리 결론은 메모리 포함 축이라 `exp7`로 분리했는데, 통합할지는 팀 1 판단 |

### 9-3. 남은 측정

- **PADE 전문(arXiv 2512.14322) 입수** 후 차별화 문장 대조
- `exp9`를 더 많은 텍스트로 확대

### 9-4. ★ 정직하게 짚을 것

**① 메커니즘 자체는 우리가 처음이 아닙니다**

Tsinghua Yin/Hu 그룹 계보가 ASIC에서 먼저 했습니다.

```
   2025-08  BETA (TCAS-II)      predictor 기반 bit-grained filter
   2025-09  MCBP (MICRO 2025)   bit-grained progressive prediction
   2025-12  BitStopper          stage-fusion + early termination
   2026-02  PADE (HPCA 2026)    predictor 제거, 순수 bound 기반   ← 가장 가깝다
```

PADE는 MSB-first, INT8, Key만 bit-serial, 부분합에 구간을 씌우는 것까지 **운용점이 같습니다.**

**남는 차별점은 6가지입니다.**

| # | 무엇 |
|---|---|
| **(d)** | **θ 정의가 다르다 — 우리는 무손실이 증명된다** ★ 가장 큼 |
| (a) | FPGA / DSP-free — ASIC 논문엔 FPGA 평가가 없다 |
| (b) | BRAM 워드폭 + work-compaction — ASIC SRAM엔 없는 문제 |
| (c) | INT8 asymmetric unsigned |
| (e) | 판정 지연이 문맥 길이의 함수 — 계보에 서술이 없다 |
| (f) | 생존 집합이 하드 top-k보다 좋다는 실측 |

**② 연산 사이클만 보면 기준선보다 8배 불리합니다**

비트평면 순차의 구조적 대가입니다. **메모리를 포함해 같은 잣대로 재야 이깁니다.**

**③ 짧은 문맥에선 절감이 거의 없습니다**

`T=128`에서 4.0%입니다. **이 설계는 긴 문맥용입니다.**

**④ `prev_step` θ 정책은 무손실이 아닙니다**

직전 스텝 θ를 재사용하면 정렬 회로를 없앨 수 있지만 **보존율이 0.9809**로 떨어집니다. 코드와 테스트에 그렇게 못 박혀 있습니다.

### 9-5. 논문에 쓸 한 문단

> 본 연구는 MSB-우선 비트평면 어텐션의 조기 종단을 **FPGA에서 특성화한다.**
> 메커니즘 자체는 PADE(HPCA 2026) 계보가 ASIC에서 선행했으나, 세 가지가 다르다.
> **첫째, 기준선 정의가 다르다** — 계보는 `max(하한) − α·radius`라는 휴리스틱을 쓰고
> 무손실을 주장하지 않는 반면, 본 연구는 **k번째로 큰 하한**을 써 top-k 무손실을
> 증명하고 1,678만 건의 상한식 검사와 384개 층·헤드·텍스트 조합에서 확인한다.
> **둘째, FPGA BRAM의 워드 단위 읽기가 절감의 실현률을 좌우한다** — 이론 절감의
> 54~100%만 실현되며, 워드폭과 스케줄 정책이 함께 정한다. **셋째, 병목 위치가
> 종단의 가치를 정한다** — 포트 수가 교차점(문맥 512에서 24, 1024 이상에서 32)을
> 넘으면 메모리 절감이 시간으로 환산되지 않는다. 이 세 가지는 SRAM 포트 폭을
> 자유롭게 정하는 ASIC 평가에서는 드러나지 않는다.

> 더 깊이: `docs/related_work.md` §0.1 (차별화 6가지 상세), §0.2 (논문용 문단)

---

## 부록 A. 문서 지도

### A-1. ★ 읽을 문서는 이거 하나입니다

```
   GUIDE.md  ← 지금 이 문서.  전체가 여기 다 있습니다
```

나머지는 **읽는 게 아니라 찾아보는 것**입니다. 필요할 때만 펴세요.

| 언제 펴나 | 문서 | 분량 |
|---|---|---|
| **RTL 짤 때** — 비트폭 유도, 메모리맵, 레지스터 전체 | `docs/ko/architecture_ko.md` | 1,029줄 |
| **실험 돌릴 때** — 명령, 옵션, 문제해결 | `docs/ko/running_experiments_ko.md` | 478줄 |
| **논문 쓸 때** — 선행연구 조사, 차별화 6가지 | `docs/related_work.md` | 614줄 |
| 저장소 구조가 궁금할 때 | `docs/structure.md` | 311줄 |
| 개념을 더 천천히 보고 싶을 때 *(선택)* | `docs/ko/background_ko.md` | 780줄 |
| `terminator_v2` 검토 결과 *(1회성)* | `docs/ko/terminator_v2_review_ko.md` | 302줄 |

### A-2. 왜 이렇게 줄였나

원래 문서가 6개였고 **같은 표가 3~5곳에 중복**돼 있었습니다.

```
   1.111배 speedup      4개 문서에 중복
   상한식 1,678만건      4개
   margin 0.40 수렴     4개
   병목 교차점           5개
```

`시스템_설명.md`(1,114줄)는 GUIDE와 **94% 겹쳤고**, `팀분업_인계.md`(350줄)는 **90% 겹쳤습니다.**
둘을 GUIDE로 흡수하고 삭제했습니다. **읽을 것이 1개, 찾아볼 것이 3개**로 정리됐습니다.

### A-3. 문서 역할 (중복 금지 규칙)

| 파일 | 역할 | 여기에만 있는 것 |
|---|---|---|
| **`GUIDE.md`** | **전체 개관** | 흐름, 결과, 분업, 함정 |
| `docs/ko/architecture_ko.md` | **사양 정본** | 비트폭 유도, 메모리맵, 레지스터 맵, 상태기계 상세 |
| `docs/ko/running_experiments_ko.md` | **실행 절차** | 명령 옵션, 규모 계산, 문제해결 |
| `docs/related_work.md` | **선행연구** | PADE 계보 분석, 차별화 6가지, 논문 문단 |
| `README.md` | 저장소 입구 | 설치, 실험 목록 |

**같은 내용을 두 곳에 적지 않습니다.** 수치가 바뀌면 GUIDE만 고치면 됩니다.

---

## 부록 B. 용어집

| 용어 | 뜻 |
|---|---|
| **비트평면 (bit-plane)** | INT8을 비트 자리별로 잘라 만든 8장의 층. index 0 = MSB |
| **`P_b`** | 평면 `b`의 부분내적. "비트가 1인 자리의 q를 더한 값" |
| **`S_m`** | 평면 `m`장 처리 후의 부분 점수 |
| **`W_m`** | 남은 비트의 최대 가중치 `2^(8−m) − 1` |
| **`Q+` / `Q−`** | `q`의 양수 합 / 음수 합. 스텝당 한 번 계산 |
| **`L_m` / `U_m`** | 하한 / 상한. `L = S + W·Q−`, `U = S + W·Q+` |
| **`θ` (theta)** | 기준선. 활성 토큰 하한 중 `k`번째로 큰 값 |
| **`K_TOP`** | 몇 개를 살릴 건가 (16) |
| **`MARGIN`** | 얼마나 세게 버릴 건가 (0.40) |
| **`M0`** | θ를 언제 확정할 건가 (평면 4) |
| **`LANES`** | 토큰 병렬도 (32) |
| **`N_MAX`** | 최대 문맥 길이 = KV 캐시 깊이 (2048) |
| **`WORD_TOKENS`** | 한 BRAM 워드에 담기는 토큰 수 (1) |
| **`alive`** | 논리적 생존 마스크 |
| **`read_live`** | **판정 지연을 반영한** 실제 읽기 마스크. 절감의 근거 |
| **keep-top-k 가드** | 생존자가 k개 미만이 되려 하면 되살리는 안전장치 |
| **판정 지연** | 종단 판정이 읽기에 반영되기까지의 평면 수. `T`의 함수 |
| **work-compaction** | 생존 토큰을 앞으로 당겨 워드 낭비를 줄이는 것 |
| **`mem_overlap`** | 읽기와 연산이 겹치는가. `max` vs `+` |
| **RoPE** | 회전 위치 인코딩. 적용 **후**를 캡처해야 한다 |
| **GQA** | Grouped-Query Attention. 질의 32 / KV 8이라 4개가 하나를 공유 |
| **crosscheck** | 골든모델 예측 vs RTL 실측 대조 |
| **돌연변이 시험** | 코드를 일부러 망가뜨려 테스트 검출력을 재는 것 |

---

*골든모델 저장소 `llm-et-attention` · 팀 1 + 팀 2 병합본*
*테스트 424개 통과 · crosscheck 12/12 · 2026-08-30 기준*
*모든 수치는 골든모델이 실제로 계산한 값이며, `source: estimate`로 표시된 하드웨어 항목은 Vivado 실측으로 교체될 자리입니다.*
