# 아키텍처 — 전체 조감도 해설

이 문서는 [slides/00_overview.svg](slides/00_overview.svg) 한 장을 글로 풀어쓴 것이다.
**설계 자체**를 설명한다. 파일과 모듈이 어디에 있는지는 [STRUCTURE.md](STRUCTURE.md)를 보면 된다.

```
        대상                        제안 설계                          검증
 ────────────────    ───────────────────────────────────    ──────────────────
 어텐션 점수 Q·K^T     ┌─────────────────────────────────┐     ┌──────────────┐
 Decode 단계          │ 비트평면 BRAM     Q+ / Q- 레지스터 │     │ 4종 비교      │
 INT8 · d = 64        │      │                │          │     │ ① ② ③ ④     │
 비트평면 저장         │      ▼                ▼          │ ──▶ └──────┬───────┘
                      │ 부분 내적·누산 ──▶ 종단 판정        │            ▼
                      │      ▲                │          │     ┌──────────────┐
                      │      └── 읽기 차단 ────┘          │     │ 측정 3층위    │
                      └─────────────────────────────────┘     └──────┬───────┘
                                                                     ▼
                                                              ┌──────────────┐
                                                              │ 결론: 경계    │
                                                              └──────────────┘
 ─────────────────────────────────────────────────────────────────────────────
 소프트웨어 검증은 RTL 없이 끝까지 진행 — 실측은 hardware.yaml · rtl_data 로 합류
```

조감도에서 **파란 테두리 상자 하나가 우리가 만드는 것**이고, 왼쪽은 전제, 오른쪽은 검증이다.
이 경계를 먼저 잡고 읽으면 나머지가 따라온다.

---

## 0. 한 문장 요약

> 어텐션 점수를 상위 비트부터 계산하다가, 이 토큰이 상위 k에 들 수 없다는 것이
> **수학적으로 확정되면** 남은 비트평면을 읽지도 계산하지도 않는다.

핵심은 "읽지 않는다"에 있다. Decode 단계는 연산이 아니라 메모리 접근이 병목이므로,
연산만 건너뛰는 기법은 이 단계에서 효과가 제한적이다.

---

## 1. 대상 — 무엇을 전제로 하는가 (조감도 1열)

### 1-1. 어텐션 점수 계산 Q·K^T

어텐션 전체가 아니라 **1단계 점수 계산만** 다룬다.
softmax 와 V 행렬 곱은 범위 밖이다(여유가 있으면 확장 과제).

### 1-2. Decode 단계 — 쓰기 1 : 읽기 N

LLM 추론은 성격이 다른 두 단계로 나뉜다.

| | Prefill | Decode |
|---|---|---|
| 처리 단위 | 프롬프트 전체 일괄 | 한 토큰씩 |
| K, V 쓰기 | N건 | **1건** |
| K, V 읽기 | — | **N건** |
| 병목 | 연산 | **메모리 대역폭** |

문헌에서 "어텐션은 가중치가 매번 바뀌므로 메모리 중심 가속기에 부적합하다"는 서술을
종종 보는데, 이는 **Prefill 이야기다.** Decode 에서 토큰 t의 K는 t단계에 한 번 저장된 뒤
이후 모든 단계에서 읽힌다. 전체 길이 T일 때 평균 재사용 T/2회이고, T=2048이면
한 번 쓰고 약 1,000번 읽는다. 저장 비용은 충분히 상쇄된다.

이 구분이 프로젝트의 가장 중요한 전제이며, 발표에서 반드시 짚어야 할 반론 방어점이다.

### 1-3. INT8 · Llama 3.2 1B · head_dim 64

계층 16, KV 헤드 8, 헤드 차원 64. 검증은 **단일 계층·단일 헤드** 수준에서 수행하고
나머지는 해석적으로 추정한다.

### 1-4. 비트평면 저장

상위 비트만 따로 읽으려면 저장 방식을 바꿔야 한다. 한 토큰의 8비트를 한 덩어리로 두지 않고
**같은 자리수끼리 모아서** 저장한다.

```
[일반]                        [비트평면]
K1 = 1 0 1 1 0 1 0 1          평면 b7 : 1 0 1 ...   ← 모든 토큰의 b7 만
K2 = 0 1 1 0 1 1 0 0          평면 b6 : 0 1 1 ...
K3 = 1 1 0 0 1 0 1 0          평면 b5 : 1 1 0 ...
```

이렇게 두면 "평면 b7만 읽기"가 하나의 연속된 메모리 접근이 되어,
상위 자리만 선택적으로 읽는 것이 물리적으로 가능해진다.

> **쓰기 경로 주의:** 새 토큰 하나의 K를 저장하려면 8비트를 8개 평면에 나눠 써야 하므로
> 쓰기가 복잡해진다. 다만 Decode 는 단계당 쓰기 1건 : 읽기 N건이므로 비중이 작다.
> 여러 토큰을 모았다가 배치로 쓰거나, 쓰기 시점에 비트를 재배열하는 소규모 버퍼를 둔다.

---

## 2. 양자화 규약 — ★ 상한식이 성립하는 전제 ★

조감도에는 안 나오지만, 2열 전체가 이 규약 위에 서 있다.
[config/quant.yaml](config/quant.yaml) 이 이를 고정한다.

### K는 반드시 unsigned 로 저장한다

```
K_stored ∈ [0, 255],   실제값 ≈ (K_stored − z) · scale
비트 자리값 = [128, 64, 32, 16, 8, 4, 2, 1]      ← 전부 양수
```

자리값이 전부 양수여야 "남은 비트가 전부 1인 경우"가 곧 최대 기여가 된다.
K를 2의 보수(signed)로 저장하면 MSB 자리값이 −128이 되어 §3-3의 상한식이 깨지고,
조기 종단이 더 이상 무손실이 아니게 된다.

`tests/test_quantize.py::test_plane_weights_all_positive` 가 이 불변식을 지킨다.

### zero-point 보정과 "곱셈기 없음"의 양립

K를 차원별(per-channel) scale로 양자화하면 정확도에 유리하지만(KIVI 권장),
scale이 내적 안쪽에 남아 곱셈이 되살아난다. 해결은 **scale을 q에 미리 접어 넣는 것**이다.

```
q̃ᵢ = q_realᵢ · scaleᵢ            ← 소프트웨어에서 스텝당 d회 (하드웨어 밖)
q̃ 를 대칭 양자화 → q_st, scale_q

s_real = scale_q · ( Σᵢ q_stᵢ·K_st[j,i]  −  Σᵢ q_stᵢ·zᵢ ) / √d
                     └── 마스크드 합, 곱셈 없음 ──┘  └ 스텝당 상수 ┘
```

두 번째 항은 **모든 토큰 j에 동일한 상수**다. 따라서

* 순위(top-k) 판정에는 영향이 없다 → 종단 로직은 정수 점수만 본다
* 최종 점수값에는 필요하다 → 누산 끝에 한 번 더한다

---

## 3. 제안 설계 — 조감도 2열 (파란 상자)

### 3-1. 비트평면 BRAM

평면 번호와 토큰 범위를 받아 해당 구간만 읽는다.
**중단된 토큰에 대해서는 읽기 요청 자체가 발생하지 않아야** 절감이 실현된다.

### 3-2. 부분 내적 · 누산

```
P_b = Σᵢ qᵢ · K_j^(b)ᵢ  =  ( K의 b번째 비트가 1인 위치의 q 값들의 합 )
S_m = Σ_{처리한 평면} 2^b · P_b
```

K의 비트가 0/1이므로 **곱셈이 필요 없다.** 마스킹 논리 + 가산 트리로 끝난다.
시프트는 배선으로 처리되므로 비용이 거의 없다.

FPGA 관점에서 이것은 **자원 종류의 교환**이다.

```
기준 설계 : INT8 곱셈기 → DSP 다수 사용, LUT 적게
제안 설계 : 조건부 덧셈 → DSP 미사용,   LUT 다수
```

DSP는 수량이 제한된 자원이고 LUT는 상대적으로 풍부하다.
이 교환이 유리한지 불리한지는 **합성 결과로 확인해야 한다.**

### 3-3. Q+ / Q- 레지스터 — 잔여 상하한

아직 읽지 않은 하위 평면들이 앞으로 기여할 수 있는 범위를 **정확히** 계산할 수 있다.

```
Q+ = q 원소 중 양수의 합
Q− = q 원소 중 음수의 합  (0 이하)

상위 m개 평면 처리 후:
    R_m = (2^(8−m) − 1) · Q+          ← 남은 평면의 최대 기여
    L_m = S_m + (2^(8−m) − 1) · Q−    ← 하한

    ⇒  L_m  ≤  s_j  ≤  S_m + R_m
```

Q+ / Q− 는 **q에만 의존**하고 q는 한 디코딩 단계 안에서 모든 토큰에 공통이다.
따라서 **스텝당 한 번만** 계산해 레지스터에 넣어 두고, 이후에는 시프트 한 번으로
매 평면의 R_m, L_m 을 얻는다. 조감도에서 이 블록이 별도로 그려진 이유다.

> 발표에서 나올 질문: "Q+/Q− 를 매 평면 계산하나요?" → 아니다. 스텝당 1회면 된다.

### 3-4. 종단 판정

```
S_m + R_m  <  θ    →    토큰 j 처리 중단
```

이 부등식이 성립하면 남은 비트를 모두 읽어도 s_j 는 θ 를 넘을 수 없다.
**근사가 아니라 손실 없는(exact) 판정이다.**

### 3-5. θ는 어디서 오는가

θ를 "계산이 끝난 토큰들의 상위 k번째 점수"로 정의하면, 모든 토큰을 평면 단위로 나란히
진행시키는 방식에서는 8개 평면을 다 처리하기 전까지 확정 점수가 하나도 없어
**종단이 단 한 번도 일어나지 않는다.**

해법은 상한과 짝을 이루는 **하한**을 함께 쓰는 것이다.

> θ = 활성 토큰들의 **하한 L_m 중 k번째로 큰 값**
>
> 하한이 θ 이상인 토큰이 최소 k개 있고 그 토큰들의 실제 점수는 각자의 하한 이상이므로
> θ 이상이다. 따라서 상한이 θ보다 작은 토큰은 상위 k에 들 수 없다.

손실 없는 판정을 유지하면서 평면을 한두 개만 처리한 시점부터 종단이 동작한다.
상한과 하한을 함께 좁혀 후보를 제거하는 이 구조는 **분기 한정(branch and bound)** 과 같다.

**θ 확정 시점은 절감량을 좌우하는 파라미터다.**

```
이르게 확정 → θ 가 낮음 → 종단 적음
늦게 확정   → θ 가 높음 → 종단 많으나 이미 읽은 양이 많음
```

구현된 정책 네 가지와 성질:

| 정책 | 설명 | 무손실 | 비고 |
|---|---|---|---|
| `every_plane` | 매 평면 갱신 | ○ | 하드웨어 비용 최대 |
| `once_at_m` | 평면 m개 시점에 한 번 확정 | ○ | m이 클수록 절감↑, 이미 읽은 양↑ |
| `prev_step` | 직전 디코드 스텝의 k번째 점수 재사용 | **✕** | 부분 정렬 회로 불필요 |
| `oracle_fixed` | 이번 스텝의 참 k번째 점수 | ○ | 구현 불가, 상한 참조선 |

> ★ `prev_step` 은 margin=0인데도 손실이 난다. θ가 이번 스텝의 하한에서 나온 것이
> 아니므로 참 top-k가 잘릴 수 있다. **회로 단순화와 무손실 보장의 교환**이며,
> 실측상 oracle의 약 90% 수준 절감을 얻는다.

### 3-6. ★ 읽기 요청 차단 — 붉은 되먹임 ★

조감도에서 종단 판정 → 비트평면 BRAM 으로 되돌아가는 붉은 화살표가
**이 설계의 존재 이유**다.

```
종단 판정 결과 = 토큰별 상태 비트
    → 연산 파이프라인 게이팅
    → ★ 메모리 읽기 요청 차단 ★
```

연산만 줄이는 기법이 Decode 단계에서 효과가 제한적인 것과 대비되는 지점이다.

**판정 지연 주의:** 평면 m의 판정은 평면 m의 가산 트리가 끝나야 나온다.
그 시점에 평면 m+1의 읽기 요청은 이미 나갔을 수 있다.
`decision_latency_planes` 를 0으로 두면 읽기 절감이 과대평가된다.

### 3-7. 정확 모드와 근사 모드

| 모드 | θ 설정 | 정확도 | 절감 |
|---|---|---|---|
| 정확(exact) | 하한 그대로 | 손실 없음 | 보수적 |
| 근사(approx) | θ + 여유값(margin) | 일부 손실 | 공격적 |

여유값을 키우면 `S_m + R_m < θ` 를 만족하는 토큰이 늘어 더 많이 중단된다.
다만 상한이 θ를 못 넘는다는 보장이 사라지므로 상위 k에 들어야 할 토큰이 잘릴 수 있다.
**그 허용 범위를 실험으로 확인하는 것**이 이 프로젝트의 주요 결과물이다.

---

## 4. 검증 — 조감도 3열

### 4-1. 4종 비교

| # | 설계 | 목적 |
|---|---|---|
| ① | 병렬 INT8 곱셈 누산 | 기준선 (DSP 사용) |
| ② | 비트평면 순차, 종단 없음 | **순차 전환 비용을 분리** |
| ③ | + 정확 모드 종단 | 손실 없는 상태의 순수 이득 |
| ④ | + 근사 모드 종단 | 절감–정확도 곡선 |

**②와 ③의 비교가 승부처다.** ②에서 순차 처리로 전환할 때 치르는 비용을 재고,
③의 종단 이득이 그 비용을 넘어서는지 확인하는 구조다.

불변식 — ① = ② = ③ 의 점수는 **비트 단위로 완전히 동일**해야 한다.
③은 무손실 판정이므로 종단을 해도 top-k 집합이 바뀌지 않는다.
`tests/test_designs.py` 가 이를 검증하며, 여기가 깨지면 이후 결과는 전부 폐기된다.

### 4-2. 측정 3층위

| 층위 | 도구 | 항목 |
|---|---|---|
| 알고리즘 | numpy / PyTorch | 평균 종단 시점, 상위 k 보존율, 언어모델 품질 |
| RTL 시뮬 | Vivado Simulator | 연산 사이클, **BRAM 읽기 횟수** |
| 논리 합성 | Vivado | LUT · FF · BRAM · DSP, 최대 동작 주파수 |
| 전력 | Vivado | 스위칭 기반 동적 전력 추정 |

### 4-3. 결론 — 이득이 비용을 넘어서는 경계

조기 종단은 공짜가 아니다.

```
비용 : 제어 논리 면적, 동작 주파수 저하, 파이프라인 빈 구간
이득 : 연산 사이클 감소, 메모리 읽기 감소
```

**사이클만 보면 안 된다.** Fmax가 함께 떨어지므로

```
실효 speedup = (cycles_기준 / cycles_제안) × (fmax_제안 / fmax_기준)
```

이 1.0을 넘는 구간을 찾아야 한다. 이 경계를 찾는 것이 프로젝트의 결론에 해당한다.

---

## 5. 팀 인터페이스 — 조감도 하단 띠

```
   [RTL / FPGA 팀]                              [소프트웨어 팀]
         │                                            │
         ├── config/hardware.yaml ────────────────────▶│   워드폭·판정지연·자원·Fmax
         ├── rtl_data/*.csv, *.rpt ───────────────────▶│   사이클·읽기·합성 보고서
         │                                            │
         └────────── utils/crosscheck.py ◀────────────┘   예측 vs 실측
```

연결은 이 두 경로뿐이다. 덕분에 RTL이 완성되기 전에도 알고리즘 검증이 끝까지 진행되고,
실측이 들어오면 예측과 자동 대조된다. 오차가 허용치(기본 5%)를 넘으면
**소프트웨어 모델이 틀린 것**이며, 원인 후보(`word_tokens`, `decision_latency_planes`,
`lanes`, `compaction_cost_cycles`)를 함께 출력한다.

RTL이 없어도 `rtl_data/mock/` 으로 이 경로 전체가 돈다.

---

## 6. 조감도에 안 그린 것 — 그러나 결론을 좌우하는 것

조감도는 한 장으로 판단하기 위해 생략한 부분이 있다. 실제로는 아래 둘이 결과를 크게 바꾼다.

### 6-1. ★ BRAM 워드 단위 읽기 ★

BRAM은 토큰을 하나씩 읽지 않는다. 한 워드에 `word_tokens` 개 토큰의 같은 평면 비트가
묶여 있으므로, **워드 안에 살아있는 토큰이 하나라도 있으면 워드 전체를 읽어야 한다.**

따라서 절감은 반드시 두 수치로 보고한다.

| 지표 | 의미 |
|---|---|
| `read_saving_ideal` | 살아있는 (토큰, 평면) 쌍 기준 — **이론** |
| `read_saving_bram` | 실제 BRAM 워드 읽기 기준 — **실현** |

이 격차가 work-compaction 이 필요한 진짜 이유다.
자세한 그림은 [slides/05_finding.svg](slides/05_finding.svg) 참조.

### 6-2. 종단 시점의 불규칙성

토큰마다 종단 시점이 다르므로 파이프라인에 빈 구간이 생긴다. 세 정책을 비교한다.

| 정책 | 방식 | 성질 |
|---|---|---|
| `batch` | 묶음 안에서 가장 늦게 끝나는 토큰에 맞춤 | 단순하나 절감 작음 |
| `compaction` | 생존 토큰을 앞으로 압축 | 절감 크나 제어 복잡 · **워드 절감을 실현하는 유일한 방법** |
| `two_phase` | 1단계 전체 스캔 → 생존 토큰만 2단계 | 중간. 분할점 m0 가 파라미터 |

---

## 7. 현재까지의 실측 (합성 데이터 기준)

`seq_len=512`, `head_dim=64`, `word_tokens=32`, `decision_latency=1`.
**경향 확인용이며, 논문 수치는 실제 캡처 텐서로 다시 뽑아야 한다.**

### 평면별 생존 곡선 — 종단은 평면 4부터 시작된다

```
평면(MSB→LSB)   0     1     2     3     4     5     6     7
생존 비율      100%  100%  100%  100%   95%   56%   18%    7%
```

→ 2단계 처리의 분할점 m0 = 4 가 데이터로 정해진다.

### 여유값 대비 절감·정확도 (top-8)

| margin | 읽기 절감(실현) | top-8 보존율 |
|---|---|---|
| 0.00 (정확) | 26.6% | **1.0000** |
| 0.70 | 45.6% | **1.0000** |
| 0.80 | 53.4% | 0.9997 |
| 0.90 | 63.2% | 0.9503 |
| 1.00 | 66.6% | 0.4339 ← 붕괴 |

### 워드폭이 실현 절감을 깎는다 (이론 27.9%)

| 워드폭 | batch | compaction |
|---|---|---|
| 1 | 27.9% (100%) | 27.9% (100%) |
| 8 | 12.3% (44%) | 27.6% (99%) |
| 32 | **3.8% (14%)** | **26.6% (95%)** |
| 64 | 1.5% (5%) | 24.4% (87%) |

### 정직하게 짚어야 할 것

* 비트평면 순차 처리는 8사이클을 쓰므로 **①보다 사이클이 8배 많다**
  (512 토큰 기준 4,305 → 34,440). 제안의 근거는 사이클이 아니라
  **DSP 미사용**과 **메모리 읽기 감소**다.
* 추정 Fmax 기준 ② 대비 실효 speedup 이 0.93 으로 손익분기 미달이다.
  Vivado 실측으로 교체하기 전까지 이 결론은 **잠정적**이다.
* 문맥이 길수록 유리하다 — 사이클 절감이 N=128 에서 −41%, N=512 에서 +10%.

---

## 8. 한계 (발표·논문에 명시할 것)

1. **합성 데이터** — torch/transformers 없이 돌 때는 합성 Q/K를 쓴다.
   attention sink 를 심고 로짓 표준편차를 실제 수준(≈5)에 맞췄으나,
   본문 수치는 `python -m src.model_hooks` 로 캡처한 실제 텐서로 다시 뽑아야 한다.
2. **RoPE 미적용** — q_proj / k_proj 출력을 캡처한다. 회전이므로 내적 분포의 스케일은
   크게 바뀌지 않으나 상대 위치 효과는 반영되지 않는다.
3. **단일 계층·단일 헤드** — 나머지는 해석적 추정.
4. **하드웨어 값은 추정치** — `config/hardware.yaml` 의 `source` 가 전부 `estimate` 인
   동안에는 §4-3의 손익분기 결론이 잠정적이다. 실행할 때마다 경고로 표시되며,
   경고가 사라지면 논문에 쓸 준비가 된 것이다.

---

## 9. 조감도 ↔ 코드 대응

| 조감도 블록 | 파일 |
|---|---|
| 비트평면 저장 | [src/quantize.py](src/quantize.py) |
| 비트평면 BRAM | [src/memory.py](src/memory.py) |
| 부분 내적 | [src/masked_sum.py](src/masked_sum.py) |
| 누산 | [src/accumulator.py](src/accumulator.py) |
| Q+ / Q- 레지스터 | [src/bounds.py](src/bounds.py) |
| 종단 판정 | [src/terminator.py](src/terminator.py) |
| θ 관리 | [src/threshold.py](src/threshold.py) |
| 읽기 차단 · 압축 | [src/schedule.py](src/schedule.py) |
| 4종 비교 | [src/designs.py](src/designs.py) |
| 디코드 루프 | [src/decode_loop.py](src/decode_loop.py) |
| 측정 | [utils/metrics.py](utils/metrics.py), [utils/cost_model.py](utils/cost_model.py) |
| 팀 인터페이스 | [config/hardware.yaml](config/hardware.yaml), [rtl_data/schema.md](rtl_data/schema.md), [utils/crosscheck.py](utils/crosscheck.py) |

---

## 관련 문서

* [README.md](README.md) — 실행 방법과 실험 목록
* [STRUCTURE.md](STRUCTURE.md) — 파일 구조와 설계 원칙
* [slides/](slides/) — 발표용 16:9 슬라이드 6장
* [rtl_data/schema.md](rtl_data/schema.md) — RTL 팀과의 데이터 계약
