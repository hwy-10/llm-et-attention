# ---------------------------------------------------------------------------
# Python
# ---------------------------------------------------------------------------
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.pytest_cache/
.venv/
venv/
env/

# ---------------------------------------------------------------------------
# 재생성 가능한 산출물 — 커밋하지 않는다
#   outputs/ : run_paper_experiments.py 로 언제든 다시 만든다
#   cache/   : 캡처한 Q/K 텐서 (수백 MB 가능)
# ---------------------------------------------------------------------------
outputs/
cache/

# ---------------------------------------------------------------------------
# 에디터 · OS · 도구
# ---------------------------------------------------------------------------
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db
desktop.ini

# Claude Code 로컬 설정 (절대 경로가 들어 있어 머신마다 다르다)
.claude/

# 편집 도구가 최상위에 남기는 작업 파일 (ops.json, ops_0.json …)
# 최상위만 막는다 — 하위 폴더의 같은 이름은 진짜 파일일 수 있다
/ops.json
/ops_*.json

# 환경변수 (HF 토큰 등)
.env
.env.*

# ---------------------------------------------------------------------------
# 커밋하는 것 (참고)
#   slides/*.svg          발표용 벡터 슬라이드
#   rtl_data/mock/*       RTL 없이 파이프라인을 돌리는 더미 데이터
#   rtl_data/*.rpt,*.csv  RTL 팀 실측 (들어오면 최상위에 놓는다)
# ---------------------------------------------------------------------------
wikitext2_test.txt
sweep_out.txt
ppl_out.txt
