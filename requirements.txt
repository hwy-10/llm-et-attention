# ---------------------------------------------------------------------------
# 필수 (코어 시뮬레이터)
# ---------------------------------------------------------------------------
numpy>=1.24,<3.0

# ---------------------------------------------------------------------------
# 선택 — 없어도 코어 실험은 전부 돌아간다 (해당 기능만 스킵)
# ---------------------------------------------------------------------------
# 그래프 생성 (utils/visualization.py)
matplotlib>=3.7
# LaTeX/CSV 표 생성 편의 (utils/io.py 는 pandas 없이도 동작)
pandas>=2.0
# config/*.yaml 파싱 — 없으면 src/config.py 의 내장 미니 파서가 대신 처리
PyYAML>=6.0
# 단위 테스트
pytest>=7.4

# ---------------------------------------------------------------------------
# 선택 — 실제 Llama 3.2 1B 텐서 캡처용 (src/model_hooks.py)
# 이게 없으면 src/dataset.py 의 합성 텐서로 전체 파이프라인이 돌아간다.
# ---------------------------------------------------------------------------
# torch>=2.0
# transformers>=4.40
# datasets>=2.18
