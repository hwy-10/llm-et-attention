# Transformer 와 Self-Attention — 배경지식

이 프로젝트가 가속하려는 연산이 **어디에서 나오는 것인지**를 정리한다.
하드웨어를 만드는 사람이 "왜 하필 Q·K^T 인가"에 답할 수 있을 만큼만 다루고,
학습(training)이나 모델 구조 전반은 다루지 않는다.

> 설계 자체는 [architecture.md](../architecture.md) 를 보면 된다. 이 문서는 그 앞단이다.

---

## 1. 한 문장으로

> Self-Attention 은 **"지금 만들 단어가 앞의 어떤 단어를 봐야 하는가"** 를
> 매번 새로 계산하는 연산이다.

"본다"의 정도를 정하는 것이 **어텐션 점수**이고, 그 점수를 만드는 것이
`Q·K^T` 다. 이 프로젝트가 다루는 것이 정확히 이 부분이다.

---

## 2. Q, K, V — 세 벡터의 역할
입력 토큰 하나의 임베딩 `x`에 서로 다른 세 개의 학습 가능한 가중치 행렬(W_Q, W_K, W_V)을 곱해 세 가지 벡터를 만든다.
```
Q = x · W_Q      Query   — "나는 무엇을 찾고 있는가"
K = x · W_K      Key     — "나는 무엇을 제공하는가"
V = x · W_V      Value   — "선택되면 내가 넘길 내용"
```


| | 역할 |
|---|---|
| **Q** | 검색어 |
| **K** | 책 제목 (검색어와 얼마나 맞는지 판정용) |
| **V** | 책 내용 (실제로 가져갈 것) |

Q 와 K 를 맞춰 보아 **얼마나 관련 있는지**를 정하고, 그 비율대로 V 를 섞는다.
Query와 Key의 일치도가 높을 때 최종적으로 전달되는 실제 의미 데이터가 V이다.

---

### 3. 계산은 네 단계다

| 단계 | 수식 / 내용 | 비고 |
| :--- | :--- | :--- |
| **① 점수** | $S = \frac{Q K^T}{\sqrt{d}}$ | $\leftarrow$ **★ 이 프로젝트의 범위** |
| **② 마스킹** | 미래 토큰 가림 (causal) | 인과적(단방향) 마스킹과 대비되는 양방향 참조 방식에 대해서는 BERT 모델을 참고하시기 바랍니다.|
| **③ 정규화** | $A = \text{softmax}(S)$ | |
| **④ 가중합** | $O = A V$ | |

> $d$는 head 차원(이 프로젝트에서는 64)이고, $\sqrt{d}$로 나누는 것은 차원이 커질수록 내적 값이 커져 $\text{softmax}$가 한쪽으로 쏠리는 것을 막기 위한 것이다.

---

### 왜 ①만 다루는가

**①에서만 "필요 없는 것을 미리 알아낼" 여지가 있기 때문이다.**

* **③ softmax**는 지수함수라 모든 점수가 다 나와야 분모를 만들 수 있다.
* **④ 가중합**은 이미 정해진 비율대로 더하는 것이라 건너뛸 대상이 없다.
* 반면 **①은 점수가 작을 것이 확실한 토큰을 계산 도중에 알아낼 수 있다.**

그리고 $\text{softmax}$는 지수함수라서 점수가 조금만 낮아도 가중치가 급격히 0에 가까워진다. 그래서 상위 몇 개 토큰이 출력의 대부분을 결정하고, 나머지는 계산해도 거의 버려진다. 이 편중이 조기 종단이 성립하는 근거다.

---

## 4. Multi-Head 와 GQA
- 헤드(Head)는 고차원 잠재 공간을 서로 다른 저차원 부분공간으로 선형 사영하여, 다양한 관점에서의 상호작용 패턴을 포착하는 독립적인 어텐션 연산 단위입니다.
- 문장: "어제 만난 그 사람은 정말 친절했다."
- 1번 헤드 (문법 전문가): "주어('그 사람')와 서술어('친절했다')의 호응 관계"에 집중
- 2번 헤드 (문맥/지시 전문가): "'그 사람'이 앞에서 누구를 가리키는지"에 집중
- 3번 헤드 (감정/수식 전문가): "'정말'이라는 부사가 '친절했다'를 꾸며주는 관계"에 집중
  
어텐션은 한 번만 하지 않고 **여러 head 로 나누어 병렬로** 수행한 뒤 다시 합친다.
head 마다 다른 관계(문법, 지시 대상, 거리 등)를 잡으라는 의도다.

최근 모델은 Q 는 head 마다 따로 두되 **K 와 V 는 여러 head 가 공유**한다.
KV 캐시 용량을 줄이기 위한 것이다.

| 방식 | K, V head 수 |
|---|---|
| MHA (Multi-Head Attention) | Q 와 동일 |
| **GQA (Grouped-Query Attention)** | Q 보다 적음 — 그룹 단위 공유 |
| MQA (Multi-Query Attention) | 1개 |

> **Llama 3.2 1B** 은 GQA 를 쓴다. 계층 16, **KV head 8**, head 차원 64.
> 이 프로젝트는 그중 **단일 계층·단일 head** 를 대상으로 검증하고
> 나머지는 해석적으로 추정한다.

---

## 5. ★ Prefill 과 Decode — 이 프로젝트에서 가장 중요한 구분

LLM 추론은 성격이 완전히 다른 두 단계로 나뉜다.

### Prefill — 프롬프트를 한꺼번에 처리

입력 프롬프트 N 개 토큰의 K, V 를 **한 번에** 계산해 캐시에 넣는다.
행렬 × 행렬 연산이라 **연산량이 병목**이다.

### Decode — 한 토큰씩 생성

새 토큰 하나를 만들 때마다,

```
새 토큰의 Q  (1개)   ×   캐시에 쌓인 모든 K  (N개)
```

즉 **벡터 × 행렬**이다. 연산량은 작은데 **읽어와야 할 K 가 많다.**
그래서 병목이 연산이 아니라 **메모리 대역폭**으로 바뀐다.

| | Prefill | Decode |
|---|---|---|
| 처리 단위 | 프롬프트 전체 일괄 | 한 토큰씩 |
| 연산 형태 | 행렬 × 행렬 | 벡터 × 행렬 |
| K, V 쓰기 | N건 | **1건** |
| K, V 읽기 | — | **N건** |
| 병목 | 연산 | **메모리 대역폭** |

### 흔한 오해 하나

문헌에서 *"어텐션은 가중치가 매번 바뀌므로 메모리 중심 가속기에 부적합하다"* 는
서술을 종종 본다. **이는 Prefill 이야기다.**

Decode 에서 토큰 t 의 K 는 t 단계에 **한 번 저장된 뒤 이후 모든 단계에서 읽힌다.**
전체 길이 T 일 때 평균 재사용이 T/2 회이고, T=2048 이면 한 번 쓰고 약 1,000번 읽는다.
저장 비용은 충분히 상쇄된다.

**이 프로젝트는 Decode 만 대상으로 한다.** 위 구분이 프로젝트 전체의 전제다.

---

### 6. KV 캐시 — 왜 저장하는가

Decode 단계에서 토큰을 하나 만들 때마다 앞서 나온 모든 토큰의 $K, V$가 필요하다. 매번 처음부터 다시 계산하면 전체 연산량이 시퀀스 길이의 제곱인 $\mathcal{O}(T^2)$에 비례하여 낭비되므로, 한 번 계산한 $K, V$를 메모리에 저장(캐싱)해 둔다.

#### 문제는 크기다

KV 캐시의 전체 메모리 사용량은 다음 수식으로 결정된다.

$$\text{KV Cache Size} = 2 \times L \times H_{KV} \times d \times T \times B_{\text{elem}}$$

* $2$: $K$와 $V$ 두 개의 텐서
* $L$: 모델의 계층 수 (Layers)
* $H_{KV}$: $KV$ 헤드 수 (GQA/MQA 적용 시 $Q$ 헤드보다 적음)
* $d$: 헤드 차원 (Head Dimension)
* $T$: 현재까지 생성된 전체 시퀀스 길이 (Tokens)
* $B_{\text{elem}}$: 원소당 바이트 수 (예: FP16은 $2\text{ bytes}$, INT8은 $1\text{ byte}$)

캐시 크기는 문장 길이 $T$에 비례하여 선형적인 $\mathcal{O}(T)$ 크기로 계속 증가하므로 메모리 병목을 해결하기 위한 여러 기법이 사용된다.

---

| 접근 방식 | 동작 원리 | 특징 |
| :--- | :--- | :--- |
| **양자화 (Quantization)** | FP16 대신 INT8 / INT4로 표현 정밀도 축소 | 메모리 점유량 감소 (본 프로젝트는 INT8) |
| **가지치기 (Pruning)** | 중요도가 낮은 토큰의 $K, V$를 캐시에서 영구 삭제 | 토큰 단위 정보 손실 발생 |
| **조기 종단 (Early Termination)** | 캐시를 버리지 않고 **하위 비트평면 읽기만 생략** | **★ 본 프로젝트의 방식: 원본 무손실 보존** |

> **조기 종단의 차별점:** 데이터를 영구 삭제(Pruning)하거나 비가역적으로 깎아내는 방식과 달리, **저장된 $K, V$ 원본을 손상시키지 않고** 내적 기여도가 낮은 하위 비트의 메모리 읽기(Load) 트래픽만 동적으로 줄인다.

---

## 7. 이 프로젝트로 이어지는 지점

여기까지가 배경이고, 이 프로젝트는 다음 관찰에서 출발한다.

1. Decode 에서 병목은 **K 를 읽어오는 것**이다 (§5)
2. softmax 때문에 **상위 몇 개 토큰만 출력에 기여**한다 (§3)
3. 그렇다면 **상위에 못 들 토큰의 K 는 끝까지 읽을 필요가 없다**

#### 핵심 메커니즘: "다 읽기 전에 수학적으로 탈락시키기"

이 아이디어를 실현하려면 **"이 토큰은 상위권 진입이 불가능하다"는 것을 데이터를 다 읽기 전에 조기에 확정**해야 한다.

* **비트 단위 계산:** $K$를 한 번에 8비트 전부 읽지 않고, 영향력이 가장 큰 **상위 비트(MSB)부터 한 비트씩 순서대로 읽으며 점수를 누적**한다.
* **수학적 상한선 판정:** 몇 개 비트만 읽은 상태에서 "남은 하위 비트가 전부 1이어도 역전이 불가능한 최대 점수(상한값)"를 수학적으로 계산한다.
* **조기 종단(Early Termination):** 상위권 진입이 불가능하다고 판정된 토큰은 **남은 하위 비트를 메모리에서 아예 읽어오지 않고 즉시 연산을 종료**한다.

> 원본 데이터는 메모리에 그대로 보존하므로 정확도 손실이 없으며, 불필요한 메모리 읽기(DRAM 트래픽)만 대폭 줄여 추론 속도를 획기적으로 개선한다.
> 
(상세 수학적 증명 및 하드웨어 구조는 [architecture.md §3](../architecture.md) 참조)

---

## 8. 용어 대조표

### 8-1. 이 문서에 나온 것

| 한국어 | English | 나온 곳 | 이 프로젝트에서 |
|---|---|---|---|
| 질의·키·값 | query, key, value | §2 | **Q 와 K 만** 쓴다. `QKSnapshot` 은 `q`, `k` 두 배열뿐이고 **V 는 코드에 없다** |
| 어텐션 점수 | attention score | §3 ① | `S = Q·K^T`. 가속 대상은 이 한 줄뿐이다 |
| head 차원 | head dim, `d` | §3 | 64. `config/model.yaml: model.head_dim` — 부분 내적의 누산 폭이기도 하다 |
| 인과 마스킹 | causal masking | §3 ② | 마스크 배열이 따로 없다. 스텝마다 앞쪽 `n_active` 개만 잘라 쓴다 (`src/decode_loop.py`) |
| 정규화 | softmax | §3 ③ | 범위 밖. 종단이 다 끝난 뒤 `src/decode_loop.py` 의 `_softmax` 가 **손실 측정용으로만** 부른다 |
| 그룹 질의 어텐션 | GQA | §4 | KV head 8개. `config/model.yaml: model.n_kv_heads` |
| 채우기 단계 | prefill | §5 | 범위 밖. 코드에는 `decode.warmup_tokens = 32` 하나로만 남는다 — "여기까지는 이미 차 있다" |
| 생성 단계 | decode | §5 | ★ 대상. **스텝 하나 = 쿼리 하나 = `StepBounds` 하나** |
| KV 캐시 | KV cache | §6 | `src/memory.py` 의 `kv_cache_bytes` · `capacity_report` |
| 양자화 | quantization | §6 | **저장 양자화만.** K 와 Q 를 INT8 로 (`src/quantize.py`). 가중치는 손대지 않는다 |

### 8-2. 설계로 넘어가면 나오는 것

여기서는 이름만 확인하면 된다. 정의는 [architecture.md](../architecture.md) 와
데모의 용어 사전 페이지(`python -m utils.visualization_example` → `/glossary`)에 있다.

| 한국어 | English | 한 줄 |
|---|---|---|
| 비트평면 | bit-plane (문헌에 따라 bit-slice) | 여러 정수의 **같은 자리** 비트를 모은 한 장 |
| 상위 비트 우선 | MSB-first | 비트평면을 b7 부터 처리 |
| 잔여 상한 | `R_m` | 남은 평면이 더 보탤 수 있는 최대치. `src/bounds.py: StepBounds.r` |
| 문턱 | θ, theta | 종단 판정의 기준선. `src/threshold.py: ThetaTracker` |
| 조기 종단 | early termination | 상한이 θ 아래로 확정되면 그 토큰은 더 읽지 않는다 |
| 분기 한정 | branch and bound | 상·하한으로 후보를 쳐내는 구조 |
| 읽기 마스크 | `read_live` | 종단 결과가 메모리 회계로 넘어가는 **유일한** 접점 |

### 8-3. ★ 이름이 겹치는 것 — 여기서는 뜻이 다르다

아래 넷은 LLM 문헌에서 **다른 뜻으로 더 자주** 쓰인다. 옮겨 읽으면 그대로 오해가 된다.

| 말 | 흔히 쓰는 뜻 | 여기서의 뜻 |
|---|---|---|
| **top-k** | 다음 단어를 뽑을 때 어휘 상위 k 개만 후보로 남기는 **샘플링** | 한 스텝 안에서 점수 상위 k 개 **토큰**. 어휘가 아니라 캐시의 토큰이고, 샘플링과 아무 관계가 없다 (`config/sweeps.yaml: top_k`) |
| **decode** | encoder–decoder 구조의 **decoder** — 모델의 절반 | 추론의 **단계** 이름 (§5). Llama 는 decoder-only 라 구조 쪽에는 가를 것이 없다 |
| **early exit** | 계층을 건너뛰고 일찍 출력 — **모델 자체가 바뀐다** | 우리 종단은 **읽기를 멈추는 것**이다. 계층도 토큰도 버리지 않는다 (§6) |
| **score** | softmax 전(`S`)과 후(`A`) 양쪽에 쓴다 | θ 와 비교하는 것은 **softmax 전 정수 `S`** 다 |

> 마지막 줄이 특히 중요하다. **종단 판정은 정수 위에서만 일어난다.**
> `s_m`, 상한, 하한, θ 가 전부 같은 정수 눈금 위에 있어서 "이 토큰은 못 든다"가
> 부등식으로 증명된다. 실수 복원(`accumulator.py: to_real_scores`)은 그 판정이
> 끝난 **뒤에** 손실을 재려고 하는 일이다.

---

## 9. 더 볼 것

### 다음에 읽을 것

| 순서 | 문서 | 여기 어느 절을 잇는가 |
|---|---|---|
| 1 | [attention_walkthrough.md](attention_walkthrough.md) §3~4 | §3 의 네 단계를 4토큰 숫자로 직접 따라간다. **§4 에서 비트평면 종단까지 간다** |
| 2 | [llama_3_2_1b.md](llama_3_2_1b.md) §4 | §6 의 KV 캐시 크기 식에 실제 숫자를 넣는다 |
| 3 | [architecture.md](../architecture.md) §1~3 | 설계. 여기부터는 배경이 아니라 우리가 만드는 것이다 |

### 원 논문 — 어디만 봐도 되는지 함께 적는다

* Vaswani et al., **"Attention Is All You Need"**, NeurIPS 2017 —
  [arXiv:1706.03762](https://arxiv.org/abs/1706.03762)
  · *Scaled Dot-Product Attention* 절만. §1·§3 의 `S = Q·K^T/√d` 가 거기서 나온다
* Shazeer, **"Fast Transformer Decoding: One Write-Head is All You Need"**, 2019 —
  [arXiv:1911.02150](https://arxiv.org/abs/1911.02150)
  · ★ **§5 주장의 원 출처.** 증분 디코딩이 왜 연산이 아니라 메모리 대역폭에 묶이는지를
  처음 수식으로 보인 글이고, MQA 는 그 결론에서 나온 처방이다 (§4 표 3행)
* Pope et al., **"Efficiently Scaling Transformer Inference"**, MLSys 2023 —
  [arXiv:2211.05102](https://arxiv.org/abs/2211.05102)
  · §5 표의 근거. prefill 과 decode 를 arithmetic intensity 로 가른다
* Ainslie et al., **"GQA: Training Generalized Multi-Query Transformer Models
  from Multi-Head Checkpoints"**, EMNLP 2023 —
  [arXiv:2305.13245](https://arxiv.org/abs/2305.13245) · §4 의 GQA
* Zhang et al., **"H2O: Heavy-Hitter Oracle for Efficient Generative Inference"**,
  NeurIPS 2023 — [arXiv:2306.14048](https://arxiv.org/abs/2306.14048)
  · §3 의 "상위 몇 개가 출력을 지배한다"를 실제로 측정한 글. 다만 **저쪽은 버리고
  우리는 안 버린다** — §6 의 가지치기와 조기 종단의 차이가 여기서 갈린다
* Liu et al., **"KIVI: A Tuning-Free Asymmetric 2bit Quantization for KV Cache"**,
  ICML 2024 — [arXiv:2402.02750](https://arxiv.org/abs/2402.02750) · §6 의 양자화

§4 의 Llama 3.2 1B 수치(계층 16 · KV head 8 · head 차원 64)는 모델 카드에서 온 것이고,
[llama_3_2_1b.md](llama_3_2_1b.md) §2 에 `config/model.yaml` 과 대조해 두었다.

### 선행연구

위 여섯은 **배경**이다. 우리 설계와 직접 겨루는 논문 — 같은 MSB-first 종단을
먼저 한 것들 — 은 [related_work.md](../related_work.md) 에 따로 정리되어 있다.
발표를 준비한다면 그쪽이 먼저다.
